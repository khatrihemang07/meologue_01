# Working input for @capacitor/assets — regenerated by
# scripts/generate-icons.sh from docs/brand/icon.svg every run, so it isn't
# committed. What actually ships lives in apps/android/app/src/main/res/.
/assets/
