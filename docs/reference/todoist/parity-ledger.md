# Todoist parity ledger

One row per nuance. This file is the checklist the Todo rebuild is measured against, and it is the
definition of done — a ticket lands when every row it owns reads `matched` or `divergent`.

Rows are generated from the reference docs in this directory as those are written, never recalled
from memory afterwards. If a behaviour has no row here, it was never established, and code that
claims to replicate it is claiming something nobody checked.

## Status vocabulary

| Status | Meaning |
|---|---|
| `todoist-captured` | We know what Todoist does. Nothing has been built. |
| `built` | meologue implements it. **Not a terminal state** — nobody has compared them. |
| `matched` | Driven in both apps side by side and compared on all three axes below. |
| `divergent` | Deliberately different. The Note column says why, and whose decision it was. |
| `blocked` | Cannot be established. The Note says what stopped it. |

`built` is called out because conflating it with `matched` is precisely how the implementation this
rebuild replaces came to diverge so widely while looking finished.

## What `matched` requires

Pixel-diffing two different applications cannot work — different content, different account,
different strings. So a row is `matched` only when all three agree:

1. **Behavioural** — same input, same state transition. The axis that decides correctness.
2. **Structural** — the DOM contract: same elements, attributes, ARIA roles and labels.
3. **Dimensional** — measured `getComputedStyle` values with both apps showing deliberately
   identical content: row height, font family/size/weight/line-height, colour as rendered `rgb`,
   padding, border-radius, checkbox diameter, indent step.

Screenshots are captured for human review and belong in `pairs/`, named `<ID>-todoist.png` and
`<ID>-meologue.png`. They are evidence for a reader, not the pass criterion — two screenshots can
look alike while the behaviour differs.

**`pairs/` does not exist yet, and no row has ever cited a file in it.** Every row reading
`matched` cites measured values instead — which is the pass criterion, and is sufficient. The
convention stands for whichever capture pass first fills it; until then, read a row's own evidence
column rather than looking for a picture nobody took. Said here because a promised-but-absent
convention is the same defect as a stale row: it invites a reader to trust something that is not
there.

## What a status is pinned to

> **Superseded by [ADR 0077](../../adr/0077-parity-is-proved-live-not-against-a-dated-capture.md).**
> Rows are now established by driving **both** applications in the same session, against Todoist's
> live behaviour, with the raw artifacts committed beside the prose. The rule below is kept because
> its reasoning was weighed and overridden rather than found wrong — read it as the argument on the
> other side, not as current practice. The short version of why it lost: pinning defended against
> drift, which never actually bit, while the real failure was pinned prose whose evidence nobody
> could re-open. Two false claims propagated out of that gap — see the ADR.

**Rows are pinned to a dated capture, never to the live product.** Todoist ships changes; this
ledger is a definition-of-done for a rebuild, and a definition-of-done cannot chase a moving
target — "done" would never be reachable.

So a row's Todoist-evidence column names the capture it was verified against
(`quick-add.md`, `pass2-2026-09-11.md`, …). A row becomes stale only when a **later capture pass
supersedes it**, at which point that pass restatuses it explicitly. A row never silently goes wrong
because Todoist shipped something.

**The worked example this rule exists for.** `SCHED-02` recorded the scheduler's quick options as
`Today · Tomorrow · This weekend · Next week`, verified against the 10 Sep capture. On 11 Sep the
live product read `Today · Tomorrow · Next week · Next weekend` — "This weekend" was gone. Nothing
in this repo broke; the reference moved. That row therefore keeps its status against the capture it
was written from, with a note pointing at `pass2-2026-09-11.md` §5.

**The cost, stated plainly:** this ledger is knowingly behind whatever Todoist shipped most
recently. That is the deliberate trade. Drift is reconciled by running a full re-capture at the
start of each parity programme and restatusing whatever moved — not by chasing changes between
programmes.

## ID scheme

| Prefix | Area |
|---|---|
| `QA-` | Quick add: recognition, withdrawal, the token vocabulary, Quick Add chrome |
| `ROW-` | Task row: anatomy, checkbox, hover, metadata line, long titles |
| `DATE-` | Date wording, tone and colour, wherever a Date renders |
| `SCHED-` | The scheduler: quick options, calendar, time, deadline, recurrence |
| `PRI-` | Priority: pickers, display, ordering |
| `DET-` | Task detail view: layout, title editing, description, sub-tasks |
| `CMT-` | Comments and the activity log |
| `KBD-` | Keyboard shortcuts, focus movement, focus appearance |
| `NAV-` | Sidebar, destinations, counts |
| `STR-` | Projects, Sections, Labels, Filters |
| `THEME-` | The design-token scope itself: palette, type, density, radii |

## Ledger

_Rows are appended as each reference doc in this directory is written._

| ID | Nuance | Todoist evidence | meologue evidence | Status | Note |
|---|---|---|---|---|---|
| QA-01 | A recognised match is an `inline-block` span with 4px horizontal padding — it occupies width, so following glyphs shift | quick-add.md § the one fact; pass2-2026-09-11.md §4 (`tod` recognised: 32.31px wide, 16px/23px) | driven in-app: `tod` recognised renders `inline-block`, padding 4px/4px, 29.41px against 21.41px withdrawn | `matched` | **Driven live in both applications in one session (`live-audit-2026-09-11.md`), the same string typed on each side — and the row matches.** Todoist 32.31px recognised / 24.31px withdrawn; meologue **32.05px / 24.05px**; delta **8.00px in both**, exactly the 4px+4px padding. Identical font stack, 16px/23px, `inline-block`→`inline`, padding 4/4→0/0 on both. Gap is **0.26px**, recorded rather than rounded away: sub-pixel, consistent across both states, and below the resolution at which two applications rendering different content can be compared at all. **The "+3.83px unreachable residue" was a measurement error, not a product fact.** The prior reading compared Todoist typing `tod` against meologue typing **`tom`** — one substituted glyph, worth ~4px at 16px, constant in both states exactly as an unchanged padding would predict. Its stated cause was also false: it held that "meologue renders Geist; Todoist renders its own stack", but `[data-surface="todo"]` declares Todoist's own stack **verbatim** and applies `font-family` unlayered on `documentElement`, beating the layered `html{font-family:Geist}` on both layer order and specificity — THEME-05, already `matched`, was right. So the handoff's "match the font stack (a far larger decision)" answered a question that never existed. History kept deliberately: #251's own reasoning (chip narrow *because* font small) was wrong too, but the 14px→22px→16px/23px token fix it shipped is what put this row in reach. Artifacts: `live-audit-dom/qa01-todoist.json`, `qa01-meologue.json`. **No screenshot** — CDP `captureScreenshot` timed out session-wide, so `pairs/` stays empty for this row |
| QA-02 | The recognised span carries `data-testid="natural-language-match"` and `data-match-id` holding the **resolved** value | quick-add.md § the span | driven in-app: `span[data-testid=natural-language-match][data-match-id]` | `matched` | One divergence: our `data-match-id` carries the raw resolved value (`2026-09-10`, `P1` for typed `p1`) where Todoist's carries a display form (`10 Sep`). Not user-visible; recorded rather than fixed. (`P4` was this row's example before #251 corrected `matchIdForToken`'s stored→UI priority inversion — see QA-12) **Driven live in both applications, same session** (`live-audit-2026-09-11.md`): both carry `data-testid="natural-language-match"` and `data-match-id`, and the format divergence was re-read live rather than carried forward — Todoist `"11 Sep"`, meologue `"2026-09-11"`. Artifact: `live-audit-dom/qa-flow-QA-02-03-06.json` |
| QA-03 | Recognised state is marked by `data-highlighted-match="true"` | quick-add.md § the span | driven in-app: `data-highlighted-match="true"` while recognised, attribute absent once withdrawn | `matched` | **Driven live in both applications, same session** (`live-audit-2026-09-11.md`): `data-highlighted-match="true"` while recognised and absent once withdrawn, identically. Artifact: `live-audit-dom/qa-flow-QA-02-03-06.json` |
| QA-04 | First Backspace after a match withdraws recognition and **deletes no character** | quick-add.md § recognition and withdrawal | driven in-app: first Backspace leaves the text `tod` unchanged and drops only the highlight | `matched` | The behaviour this whole programme started from, now reproduced. **Driven live in both applications, same session** (`live-audit-2026-09-11.md`): the first Backspace leaves `tod` intact and drops only the highlight, in both. Artifact: `live-audit-dom/qa-flow-QA-04-05.json` |
| QA-05 | Second Backspace deletes a character and removes the span | quick-add.md § recognition and withdrawal | driven in-app: second Backspace yields `to` and removes the span | `matched` | **Driven live in both applications, same session** (`live-audit-2026-09-11.md`): the second Backspace yields `to` and removes the span, in both. Artifact: `live-audit-dom/qa-flow-QA-04-05.json` |
| QA-06 | Withdrawn span persists as the **same element**, restyled to `inline` with no padding | quick-add.md § the span | driven in-app: the same element persists through withdrawal, restyled from inline-block to inline with padding dropped | `divergent` | **Settled by a MutationObserver tiebreak — the two applications differ, structurally only.** (`live-audit-2026-09-11.md`, 3 runs per app.) A unique expando token was set on the recognised span and a `MutationObserver` watched the editor through one Backspace. **Todoist replaces the node**, 3 of 3: the held span ends detached (`heldIsConnected: false`, `sameAsHeld: false`), the span now in the editor lacks the token, and the log records a `childList` *adding* a new `SPAN[data-testid=natural-language-match]` and a text node, then *removing* the old span. **meologue restyles the same node**, 3 of 3: still connected, `===` the held reference, token intact, and only `attributes` mutations (`data-highlighted-match`, `class`). So `quick-add.md`'s **verified** claim that the span "is *not* created and destroyed around recognition… one element in two visual states" is **false for Todoist** (corrected there). This supersedes the earlier single tag probe, which was right but too weak to overturn a load-bearing claim on its own. **Nothing user-visible differs**: text, width, padding and the two-step Backspace all matched live (QA-01, QA-04, QA-05). `divergent` here is "compared and found different", and **the user decided on 2026-09-12 to match Todoist.** The recommendation had been to keep meologue's node identity (no behavioural upside to recreating a node, and stable identity is the more useful DOM contract); the user chose strict DOM parity instead. So this is **not** a deliberate divergence — it is a fix candidate: meologue's withdrawal should replace the recognised span with a new node, as Todoist does, while keeping every observable (text, `data-match-id`, width, two-step Backspace) unchanged. Artifacts: `live-audit-dom/qa06-tiebreak-todoist.json`, `qa06-tiebreak-meologue.json` (tiebreak); `qa-flow-QA-02-03-06.json`, `qa-flow-todoist-qa06-identity-retest.json` (first probe) |
| QA-07 | Withdrawal is per-occurrence and one edit deep — editing back to the same text re-recognises | quick-add.md § withdrawal is not sticky | `todo-quick-add-recognition.ts` `remapWithdrawnSpans` — positional, not signature-keyed (deliberately the opposite of `quick-add-highlight.ts`'s own model) | `matched` | **Driven live in both applications, same session, identical input** (`live-audit-2026-09-11.md`): from a withdrawn `tod`, typing `x` → `todx` clears the span in both; Backspacing the `x` → `tod` re-recognises in both. The end-to-end browser verification this row's note said was missing is now done. Artifact: `live-audit-dom/qa-flow-QA-07.json` |
| QA-08 | Moving the caret into a recognised span does not withdraw it | quick-add.md § other ways to reject | `todo-quick-add-recognition.ts` `quickAddRecognitionPlugin` only reacts to `Backspace` in `handleKeyDown`; a caret move alone never touches its state | `matched` | **Driven live in both applications, same session** (`live-audit-2026-09-11.md`): `Home` then `ArrowRight` moves the caret into the recognised span and it stays highlighted, in both. Artifact: `live-audit-dom/qa-flow-QA-08.json` |
| QA-09 | Date vocabulary: today/tod/tomorrow/tmr/weekday/next week/in N days/25 dec/12/25 | quick-add.md § vocabulary | `en.ts:61-70` (relative-day words); `date-rules.ts:165-186` (weekday), `:206-226` (in N days), `:241-260` (next week), `:94-136` (25 dec / 12/25) | `divergent` | **Two real gaps, found by driving both vocabularies live with identical strings** (`live-audit-2026-09-11.md`). As with `CMT-04`/`CMT-05`, `divergent` here marks "compared and found different", **not** a design choice — both are defects and both are fix candidates. (1) **`12/25` is not recognised at all in meologue**, where Todoist resolves it to 25 Dec — a parse gap, not a formatting difference, despite `date-rules.ts:94-136` claiming to cover it. (2) **`next week` resolves to a different day**: Todoist gives the *next Monday* (14 Sep), meologue gives *today+7* (18 Sep, a Friday). Every other form in the list — today/tod/tomorrow/tmr/monday/friday/in 3 days/25 dec — agreed. Artifact: `live-audit-dom/qa-flow-QA-09-10-11-12.json` |
| QA-10 | Time vocabulary: `at 5pm`, `5pm`, `17:00` | quick-add.md § vocabulary | `quick-add/date-rules.ts` `matchExplicitTime` — the optional `at`, the 12-hour-plus-meridiem branch and the 24-hour `HH:MM` branch; reaches a Task through `taskFieldsFromQuickAdd` (`lib/quick-add-task.ts`) and is decorated as a `time` match (`lib/todo-quick-add-recognition.ts`) | `divergent` | **Structural gap, driven live with identical strings** (`live-audit-2026-09-11.md`). All three forms are recognised on both sides, but they resolve to different *kinds* of value: Todoist's `data-match-id` bundles a resolved date with the time (`"12 Sep 5:00 PM"`), meologue's carries the bare time only (`"17:00"`). So a time-only phrase implies a day in Todoist and implies nothing in meologue. `divergent` in the `CMT-04` sense — compared and found different, a fix candidate rather than a decision. Note this is the recognition chip's identifier, not necessarily what `taskFieldsFromQuickAdd` ultimately stores; the storage side was not driven here. Artifact: `live-audit-dom/qa-flow-QA-09-10-11-12.json` |
| QA-11 | Recurrence vocabulary, with `daily` normalised to "every day" | quick-add.md § vocabulary | `date-rules.ts:375-388` (`matchRecurrenceWord`); `en.ts:116-124` (`recurrenceWords` table); `quick-add-task.ts:123-131` (`RECURRENCE_WORD_TO_PHRASE`, `daily` → `"every day"`) | `matched` | **Driven live in both applications, same session, identical strings** (`live-audit-2026-09-11.md`): `every day`, `every monday` and `daily` all recognise on both sides, with `daily` normalised to "every day" in both. Artifact: `live-audit-dom/qa-flow-QA-09-10-11-12.json` |
| QA-12 | `p1`–`p4` recognised as priority, showing no date control | quick-add.md § vocabulary | `quick-add/rules.ts` `matchPriority` (`/\bp([1-4])\b/gi` through `storedPriorityOf`), decorated as a `priority` match by `todo-quick-add-recognition.ts` `matchIdForToken`, which crosses the stored→UI inversion back via `uiPriorityOf` so typed `p1` carries `data-match-id="P1"` and typed `p4` carries `"P4"` | `matched` | **Status was stale.** The second clause ("showing no date control") is true only vacuously: Quick Add has no footer controls of any kind to show or hide — see QA-15, which is the row that actually owns that gap. **#251:** `matchIdForToken` previously read the stored priority number directly, so typed `p1` (stored 4) emitted `data-match-id="P4"` — the inverted identifier, invisible only because nothing rendered a chip from it yet. Now crossed via `uiPriorityOf`, confirmed independently by `docs/reference/todoist/rename-capture-2026-09-11.md` (`priority_4` is Todoist's own storage class for UI "P1"). **Driven live, and the format diverges** (`live-audit-2026-09-11.md`): `p1`–`p4` recognise on both sides, but Todoist's `data-match-id` carries a **bare digit** (`"1"`–`"4"`) where meologue carries `"P1"`–`"P4"`. Worth stating plainly — #251 deliberately changed this identifier and moved it *away* from Todoist's actual format, while believing it was correcting an inversion. The inversion fix itself was right; the `P` prefix is ours. Not user-visible, and the same class of difference QA-02 already carries while reading `matched` — so this row reads `matched` too, for consistency, rather than one row being judged strictly and the other generously. Artifact: `live-audit-dom/qa-flow-QA-09-10-11-12.json` |
| QA-13 | `#` project popup, filtering, with "Project not found. Create X" fallback | quick-add.md § autocomplete; `live-audit-2026-09-11.md` (re-driven live: `#` opens a `role="listbox"` dropdown, and a non-matching query offers to create the project) | not built — meologue has no `#` popup | `todoist-captured` | Todoist's side re-confirmed live; there is nothing on meologue's side to compare. **A second Todoist near-miss happened on this row:** typing `#zzzznonexistent` too fast let focus fall out to `body`, and the trailing keystrokes fired Todoist's global shortcuts, navigating to `/app/notifications`. Nothing was created and the Inbox canary held at 16. In autocomplete tests, assert focus after *every* keystroke, not only before the first. Artifacts: `live-audit-dom/qa-flow-QA-13-14.json`, `qa-flow-todoist-qa13-14-final.json` |
| QA-14 | `@` label popup with "Label not found. Create X" fallback | quick-add.md § autocomplete | `rules.ts` `matchLabel` now matches `@label`, not `%label` (`%` dropped, not kept as an alias) | `built` | #226's first half (this row's parser side only): `packages/core/src/quick-add/` recognises `@` as the label sigil. The autocomplete popup itself is untouched — #226's own follow-up half. **Stale caveat dropped:** this note used to claim "apps/web's own `%`-sigil assumptions … are untouched"; `promote-tasks.ts:93-104` already routes labels through the identical `@`-based parser (`taskFieldsFromQuickAdd`), and no `%` sigil exists anywhere in `quick-add-task.ts` or `promote-tasks.ts`. **Todoist's side re-driven live** (`live-audit-2026-09-11.md`): `@` reuses the same `role="listbox"` dropdown as `#`, with a create-label fallback. meologue still has the parser half only and no popup, which is exactly why this row stays `built`. Artifact: `live-audit-dom/qa-flow-QA-13-14.json` |
| QA-15 | Footer controls mirror the parse and gain Remove buttons once set | quick-add.md § Quick Add chrome | not built — meologue's add field has no footer controls | `todoist-captured` | **Re-driven live on the actual Quick Add dialog** (`live-audit-2026-09-11.md`; identity asserted: `role="dialog"`, `aria-label="Quick Add"`, one match). With `tod` recognised, the footer's aria-labelled buttons read, in DOM order, **More actions · Remove date · Cancel · Add task** — so "a Remove control appears once a date is parsed" is re-confirmed on the dialog itself. **A correction to this row's own earlier note:** it argued that a first run's identical four-button footer proved that run was on the inline row, because the dialog "also carries Select project, Set priority and Add labels". The real dialog shows the same four aria-labelled buttons, so the footer was never evidence of which surface was open (the 774px editor width, recorded under QA-16, still is). **Not established:** that read collected buttons by `aria-label`, so it cannot say whether unlabelled project/priority/label controls still exist — do not read this as Todoist having removed them. Artifacts: `live-audit-dom/quickadd-dialog-todoist.json` (dialog), `qa-flow-todoist-qa15-18.json` (the earlier inline-row read) |
| QA-16 | Quick Add is 580×66px at rest and 580×97px once a date is recognised, radius 12px, padding 16px | quick-add.md § Quick Add chrome | — | `todoist-captured` | Dark theme values. **Trap for whoever builds this:** `--td-composer-background`/`-border`/`-radius` are already declared in `index.css` and have **zero consumers anywhere in the repo** — the real Quick Add is a full-width bordered row holding a 32px field (`add-task-form.tsx`). A declared token is not an implementation, which is the same class of defect as `@theme inline` baking `--font-sans` (THEME-05's own note). **A 2026-09-11 live run produced 800×89px, radius 12px, `rgb(40,40,40)`, border `1px solid rgb(112,112,112)`, padding 12px — and those numbers are NOT recorded here, because the element measured was almost certainly the wrong one.** It reported `position:relative` and **no `role="dialog"`**, where this row's own source (`quick-add.md` § Quick Add chrome) defines Quick Add as `role="dialog" aria-label="Quick Add"` opened by `.plus_add_button`. That reads like Todoist's *inline* add-task row, a different surface. Recording it would repeat precisely the uncontrolled-measurement error QA-01 was just corrected for. **Re-measure by opening `.plus_add_button` and asserting `role="dialog"` on the measured node before reading geometry.** **Checked against the artifact afterwards, and it is worse than a wrong element: the reported numbers are not in it at all.** `live-audit-dom/qa-flow-todoist-qa16-geometry.json` holds only the ProseMirror editor and seven wrapper `DIV`s, every one 774×23px with `0px` radius, transparent background and `0px` padding. No node in it is 800×89, radius 12 or `rgb(40,40,40)`; those figures appear only in the agent's prose summary, with nothing committed behind them. The 774px editor width does confirm *which* surface was open — it cannot fit a 580px dialog, so this was Todoist's inline add-task row in the 800px column. Kept as a record of what went wrong, not as evidence for any geometry. **Re-measured on the real dialog, and every value is in the artifact** (`live-audit-dom/quickadd-dialog-todoist.json`, identity asserted `role="dialog"` `aria-label="Quick Add"`, one match): at rest **580×66px**; with `tod` recognised **580×97px**; both states radius `12px`, padding `16px` on all four sides, border `1px solid rgb(61,61,61)`, background `rgb(40,40,40)`, shadow `rgba(0,0,0,0.2) 0 4px 8px`. This row's old wording ("580×97px at rest") was out of date — `pass2-2026-09-11.md` §4 had already measured 66 empty and 97 recognised — and the nuance column now says so. **Why the first run missed:** the corpus told it to open Quick Add with `.plus_add_button`, which on this account is the *in-list inline* add trigger, not the global opener; the real opener is the sidebar's "Add task" button, which has no aria-label. Still `todoist-captured`: meologue has no Quick Add dialog to compare (NAV-12, `divergent`) |
| QA-17 | The title field is the only contenteditable — no description input by default | quick-add.md § Quick Add chrome | `add-task-form.tsx` renders the shared `TaskTitleEditor` with `quickAddRecognitionPlugin` | `matched` | **This row was stale and said the opposite.** It claimed the field was still a plain `<input>`, which was true when #225 deliberately deferred the conversion and false the moment #226 completed it. Caught by the branch's own spec review, not by any test — a ledger row asserting something untrue is worse than a missing one, since the whole file exists to be trusted. **Driven live in both applications, same session** (`live-audit-2026-09-11.md`): a single `contenteditable` at rest on both sides, no description input in either. Artifact: `live-audit-dom/qa-flow-QA-17.json` |
| QA-18 | `Tab` from the title moves to More actions | quick-add.md § Quick Add chrome | not built — no equivalent chrome | `todoist-captured` | **Re-driven live on the actual Quick Add dialog** (`live-audit-2026-09-11.md`; identity asserted `role="dialog"` `aria-label="Quick Add"`). With the title field focused and empty, one `Tab` focused a `BUTTON` with `aria-label="More actions"` — confirmed now on the surface this row names, and earlier on the inline add-task row too, so both of Todoist's task-creation surfaces agree. Nothing on meologue's side to compare. Artifacts: `live-audit-dom/quickadd-dialog-todoist.json` (dialog), `qa-flow-todoist-qa15-18.json` (inline row) |
| QA-19 | `Shift+Enter` **submits** rather than inserting a newline | quick-add.md § Shift+Enter | `task-title-editor.tsx`'s keymap binds `Shift-Enter` to `options.commit()`, with a comment citing this row by name; `add-task-form.tsx` wires `onCommit` to the submit path | `built` | **Status was stale** — #225 built this and cited QA-19 while doing so, and the row was never updated. User had independently flagged this. **Deliberately NOT driven live on Todoist's side, so this row cannot read `matched`** (`live-audit-2026-09-11.md`): pressing `Shift+Enter` in Todoist *submits*, which would create a real task in the user's live account, and this programme's safety rule forbids that. meologue's half was driven — `Shift+Enter` submitted, creating a task that was then deleted — and Todoist's half rests on `quick-add.md`'s original accidental discovery. ADR 0077 requires both sides driven in one session; one side here is a captured claim, not a live reading, and saying so is the point |
| QA-20 | Quick Add title renders at 16px/23px against 13px chrome | quick-add.md § fonts | Both sides now built: the 13px chrome is `--td-chrome-font-size`, applied in `todo-sidebar.tsx`; the Quick Add title is `--td-composer-title-font-size`/`-line-height` (16px/23px), now read by `add-task-form.tsx`'s `EDITOR_BOX_CLASSES` (`text-[length:var(--td-composer-title-font-size)] leading-[length:var(--td-composer-title-line-height)]`) at every width instead of a hardcoded `md:text-sm md:leading-[22px]` beside the unused token | `matched` | **#251 fixed the dead-token trap this row recorded** — the title tokens had zero consumers before. Not `matched`: nobody has driven the two Quick Adds side by side with a real-browser pixel measurement to confirm the computed sizes agree, only that the component now consumes the declared tokens. See QA-16 for the still-open composer-chrome instance of the same trap (different tokens: background/border/radius, untouched by this fix). **Now `matched`: driven live in both applications, same session** (`live-audit-2026-09-11.md`) — the title renders 16px/23px in both, on the byte-for-byte identical system font stack, the real-browser confirmation this note said was outstanding. Artifact: `live-audit-dom/qa-flow-QA-20.json` |
| QA-21 | Arrow-key navigation inside autocomplete popups | quick-add.md § autocomplete | — | `blocked` | Capture inconclusive; not evidence of absence |
| QA-22 | Deadline syntax in Quick Add | — | — | `blocked` | Never reached |
| QA-23 | Plain `Enter` isolated from `Shift+Enter` | — | — | `blocked` | Stopped after the account mutation |
| QA-24 | Growth behaviour with a 300+ character title | — | — | `blocked` | Never reached |
| DATE-01 | Overdue renders a relative word ("Yesterday") in `rgb(255,112,102)` with a calendar icon | scheduler-and-priority.md §9 | `format-task-date.ts` `formatTaskDate`/`describeDay`; `--td-date-overdue` | `built` | No calendar icon on the date badge itself (task-row-content.tsx) — the row's own metadata line carries no icons except comment/sub-task counts. **Wording and colour matched live (flow 2):** "Yesterday" in `rgb(255,112,102)` in both. The calendar-icon clause was not re-read on Todoist, so the row stays `built` on that half. Artifacts: `live-audit-dom/flow2-DATE-01-03-06-09-10-11-todoist.json`, `flow2-DATE-01-03-06-09-10-11-meologue.json` |
| DATE-02 | Completion **overrides** the date tone — a completed overdue row mutes to `rgb(204,204,204)` | scheduler-and-priority.md §9 | `format-task-date.ts` `formatTaskDate({ completed })`; `--td-date-muted` | `matched` | **Driven live on both sides (flow 2):** a completed Task dated yesterday renders its date `rgb(204,204,204)` in both, not the overdue red. Artifacts: `live-audit-dom/flow2-ROW-14-15-DATE-02-todoist.json`, `flow2-ROW-14-15-DATE-02-meologue.json` |
| DATE-03 | Within the next 7 days renders the **weekday name only**, in `rgb(169,112,255)` | scheduler-and-priority.md §9 | measured in-app: a task 4 days out renders `Monday` in `rgb(169,112,255)` | `matched` | Driven side by side; weekday-name-only confirmed. **Re-driven live on both sides (flow 2):** a Task 4 days out reads "Tuesday" in `rgb(169,112,255)` in both. Artifacts: `live-audit-dom/flow2-DATE-01-03-06-09-10-11-todoist.json`, `flow2-DATE-01-03-06-09-10-11-meologue.json` |
| DATE-04 | A recurring task appends ↻ after the date, same colour | scheduler-and-priority.md §7 | `format-task-date.ts` `formatTaskDate({ recurring })` | `divergent` | The literal `task.dateString` rule still renders as its own separate badge alongside the ↻'d date (task-row-content.tsx) — additive, not a replacement. **Compared live on each app's Today view (flow 2), and they differ:** for a recurring task due today, Todoist keeps the date control as an **icon-only** badge — the recurrence glyph in Today green `rgb(37,184,76)`, text emptied — while meologue suppresses the resolved date and keeps a separate `every day` **text** badge. On the Inbox view meologue renders `Today ↻` plus `every day`; Todoist's Inbox rendering of the same task was not read, so the non-Today case is still open. Artifacts: `live-audit-dom/flow2-ROW-13-todoist.json`, `flow2-ROW-13-meologue.json`, `flow2-DATE-04-DATE-05-NAV-05-meologue.json` |
| DATE-05 | Upcoming day headings read `10 Sep ‧ Today ‧ Thursday`, separator U+2027; only today/tomorrow get a relative word | scheduler-and-priority.md §9 | `task-views.ts` `upcomingDayHeading()` (NAV-05); `format-task-date.ts` now shares the identical Today/Tomorrow words for `Task.date` itself | `matched` | **Driven live on both sides (flow 2):** Upcoming reads `11 Sep ‧ Today ‧ Friday` and `12 Sep ‧ Tomorrow ‧ Saturday` in both, with the U+2027 separator. meologue's further headings (`15 Sep ‧ Tuesday`, `21 Sep ‧ Monday`) had no Todoist counterpart read that session. Artifacts: `live-audit-dom/flow2-DATE-05-NAV-05-todoist.json`, `flow2-DATE-04-DATE-05-NAV-05-meologue.json` |
| DATE-06 | Row wording for a task due exactly today / tomorrow | pass2-2026-09-11.md §1 (Due today → `Today`, Due tomorrow → `Tomorrow`, both verified) | `format-task-date.ts:129` (`"Today"`), `:132` (`"Tomorrow"`) | `matched` | **Unblocked** — pass 1 found no such task in the account; the account has since changed and pass 2 measured both, confirming the wording this ticket already implemented. Not `matched`: nobody drove the two apps side by side, and the row's own colour half is DATE-09, now falsified — see that row. **meologue read live (flow 2):** "Today" and "Tomorrow" for tasks due those days. Todoist's two rows were **not** read that session — its list is virtualized and kept them outside the rendered window — so this is still a one-sided reading. Artifact: `live-audit-dom/flow2-DATE-01-03-06-09-10-11-meologue.json`. **Now read on both sides, same session (flow 3), each row scrolled into view first:** "Today" and "Tomorrow" in both. Artifacts: `live-audit-dom/flow3-DATE-06-09-10-todoist.json`, `flow3-DATE-06-09-10-meologue.json` |
| DATE-07 | Row rendering of a date carrying a time | live flow 3: a task due tomorrow at 9:30 renders **"Tomorrow 9:30 AM"** in `rgb(255,154,20)` (`live-audit-dom/flow3-DATE-06-09-10-todoist.json`) | see DATE-10 | `divergent` | **Unblocked** by a disposable timed fixture, created and then deleted. Todoist appends the time after a plain space, with no comma; meologue's rendering differs by exactly that comma, which DATE-10 owns. Previously `blocked`: no timed task existed in the account |
| DATE-08 | Row rendering of a Deadline | — | — | `blocked` | **Deadline is Pro-only**; account is Free. `formatDay` (Task.deadline's own formatter) deliberately stays tone-less — see `format-task-date.ts`'s own header comment |
| DATE-09 | Row wording/colour for a Task due exactly today or tomorrow | pass2-2026-09-11.md §1 (Due today → `Today`, `rgb(37,184,76)` green; Due tomorrow → `Tomorrow`, `rgb(255,154,20)` orange; verified four times) | `format-task-date.ts`: `DateTone` (`:70`) now has its own `"tomorrow"` member, no longer folded into `"upcoming"`; `TONE_COLOUR` maps `today` → `--td-date-today` (`rgb(37,184,76)`) and `tomorrow` → `--td-date-tomorrow` (`rgb(255,154,20)`), both declared in `index.css`'s `[data-surface="todo"]` scope | `matched` | **Issue #250 fixes the falsification this row previously recorded.** The guess (reusing `--td-date-upcoming` for Today, with no tone at all for Tomorrow) is replaced by the two measured colours above. **Dark theme only** — pass2-2026-09-11.md §1 is explicit these two colours are theme-dependent (Todoist's own "Todoist" theme reads `rgb(113,250,149)`/`rgb(255,180,83)` for the identical two states); DATE-12 tracks that still-open gap, untouched by this fix. Not `matched`: implemented and unit-tested (`format-task-date.test.ts`), but nobody has driven the two apps side by side in a real browser to confirm the computed colours — jsdom computes no layout or cascade, so that step is still outstanding. **meologue's half is now read live (flow 2):** Today `rgb(37,184,76)`, Tomorrow `rgb(255,154,20)`, the values pass 2 measured in Todoist — but Todoist was not re-read in the same session (its rows were virtualized out), so ADR 0077's bar is not yet met. Artifact: `live-audit-dom/flow2-DATE-01-03-06-09-10-11-meologue.json`. **Now read on both sides, same session (flow 3):** Today `rgb(37,184,76)` and Tomorrow `rgb(255,154,20)`, identical, read from Todoist's painted inner `<span>` rather than its button. Dark theme only — DATE-12 still owns the theme axis. Artifacts: `live-audit-dom/flow3-DATE-06-09-10-todoist.json`, `flow3-DATE-06-09-10-meologue.json` |
| DATE-10 | Row wording for a Date carrying a time | — | `format-task-date.ts` `formatTaskDate` appends `", h:mm a"` after the resolved day word, e.g. "Tomorrow, 9:30 AM" | `divergent` | **Driven live on both sides, same session (flow 3) — the wording differs by one comma.** Todoist renders **"Tomorrow 9:30 AM"**; meologue renders **"Tomorrow, 9:30 AM"**. Colour `rgb(255,154,20)` in both. So the `", h:mm a"` this row built as "a reasonable extension" adds a comma Todoist does not have. Fix candidate. Artifacts: `live-audit-dom/flow3-DATE-06-09-10-todoist.json`, `flow3-DATE-06-09-10-meologue.json`. **Earlier note:** **Unverified** — DATE-07 is `blocked`, no timed Task was ever observed. A reasonable extension of the existing all-day wording, not a replay of a measured value. **meologue read live (flow 2):** "Tomorrow, 9:30 AM" in `rgb(255,154,20)`. (Flow 2's timed Todoist row was scrolled out of view before it was read; flow 3's was read — see above.) Artifact: `live-audit-dom/flow2-DATE-01-03-06-09-10-11-meologue.json` |
| DATE-11 | Row wording for a Date more than 6 days out, or more than 1 day overdue ("further out / next year") | scheduler-and-priority.md §9 ("further out / next year," gap) | `format-task-date.ts` `describeDay` falls back to the plain absolute `formatDay`, toned `none` (upcoming side) or `overdue` (overdue side) | `divergent` | **Driven live on both sides (flow 2) — the wording order differs.** A Task 10 days out reads **"21 Sep"** in Todoist and **"Sep 21"** in meologue; colour `rgb(204,204,204)` in both. Day-then-month is Todoist's order everywhere else as well (Upcoming headings, the captured "1 Sep"). Fix candidate. Artifacts: `live-audit-dom/flow2-DATE-01-03-06-09-10-11-todoist.json`, `flow2-DATE-01-03-06-09-10-11-meologue.json`. **Earlier note:** **Unverified**, but not unconstrained: the captured account's one completed, 9-days-overdue Task rendered as the plain "1 Sep," not a second relative phrase (scheduler-and-priority.md §9) — this fallback matches that data point even though the ticket that produced it never set out to establish this row |
| DATE-12 | Date colours are **theme-dependent** — the same Task's Today/Tomorrow colour differs between Todoist's own named themes | pass2-2026-09-11.md §1 (Today `rgb(113,250,149)` under the "Todoist" theme vs `rgb(37,184,76)` under Dark; Tomorrow `rgb(255,180,83)` vs `rgb(255,154,20)`) | `format-task-date.ts`'s `TONE_COLOUR` (`:72`) is a single static map with no theme parameter | `todoist-captured` | New row — no existing row tracked this axis. Nothing in meologue's `THEME-*` scope varies a Date colour by theme; any replica hard-coding one set of date colours (as `format-task-date.ts` does) only ever matches one Todoist theme, and closing this needs a decision on whether Todo's theme surface should carry per-theme date tokens at all |
| SCHED-01 | Anchored popover, 250×525px, radius 10px, background `rgb(38,38,38)`, border `1px solid rgb(61,61,61)` | scheduler-and-priority.md §1 | `task-schedule-popover.tsx`; `ui/popover.tsx`; `--td-popover-*` (index.css) | `built` | Dark theme; light untested. Measured live via ego-browser: `getComputedStyle` on the rendered card read back exactly 250×525px, `10px` radius, `rgb(38,38,38)` background, `1px solid rgb(61,61,61)` border, and the two-layer shadow below, byte for byte. **Issue #253 fixed the "reaching it" half this row previously recorded as broken.** The bottom sheet and the "Pick a date" button it gated the popover behind are both gone: the row's hover Date button, the detail view's Date attribute, the More-actions "Date…" item and the `T` shortcut now all anchor this same popover directly, via a per-site Radix `Popover.Trigger` (each real ref-forwarding — `task-row-content.tsx`'s own doc comment on why that specifically matters). **Anchoring is now measured, and one entry point is broken.** `verification-2026-09-11.md`: all four entry points were driven in a real browser and the popover's rect was read against its trigger's rect — row hover Date button, More-actions "Date…", the `T` shortcut and the detail view's Date attribute each anchored directly under their own control, none at the viewport origin, at 250×525 and 10px radius. Set date, clear date and set recurrence were exercised from two different entry points. **But the More-actions "Date…" item does not reliably open it by MOUSE: measured 2 of 10 attempts, against 3 of 3 by keyboard, deterministic by row position (bottom rows open, upper rows never do) rather than intermittent — filed as issue #255 with the evidence and two reverted fix attempts.** Stays `built`, not `matched`: three of four entry points behave, one does not, and a row cannot claim a match while a reader clicking a menu item mostly gets nothing. **Popover geometry re-read live on both sides, same session (flow 3):** 250×525, radius 10px, `rgb(38,38,38)`, `1px solid rgb(61,61,61)`, and the identical two-layer shadow `rgba(0,0,0,0.19) 0 10px 20px, rgba(0,0,0,0.23) 0 6px 6px`. meologue's popover opened at 1123,309, directly under its 44×44 trigger at 1123,261. Todoist's trigger rect was not captured, so anchoring is still compared on meologue alone, and #255 still keeps this row `built`. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-01-meologue-raw.json` |
| SCHED-02 | Quick options in order: Today `Thu` · Tomorrow `Fri` · This weekend `Sat` · Next week `Mon 14 Sep` | scheduler-and-priority.md §2 | `task-schedule-popover.tsx`'s `QuickOption` list | `divergent` | **Driven live on both sides on Saturday 12 Sep (flow 3), and slots 3–4 differ.** Todoist: Today (Sat) · Tomorrow (Sun) · **Next week (Mon 14 Sep) · Next weekend (Sat 19 Sep)**. meologue: Today (Sat) · Tomorrow (Sun) · **This weekend (Sat) · Next week (Mon 14 Sep)** — and on a Saturday its "This weekend" hint simply repeats Today's. The run's own note called this "environmental" drift because the two apps were read a session apart; they were read on the same day, and ADR 0077 makes Todoist's live product the reference, which settles the decision the earlier note below said was unmade. Fix candidate. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-content-meologue.json`. **Earlier note:** Next week shows a full date, others a weekday. Verified in-app on the reference instant (Thu 10 Sep 2026): hints read exactly Thu/Fri/Sat/Mon 14 Sep. Status left unchanged: pass2-2026-09-11.md §5 records that Todoist's own slots 3/4 changed (`This weekend`/`Next week` → `Next week`/`Next weekend`) between 10 and 11 Sep — a live-product change, not a code defect, and restatusing depends on an unmade decision (chase Todoist's live product, or pin to a capture date) |
| SCHED-03 | "No Date" appears **only once a date is already set** | scheduler-and-priority.md §2 | `task-schedule-popover.tsx` (`dateDay !== null` guard) | `built` | **Todoist re-driven live (flow 3):** "No Date" is absent from an undated draft and present once `tod` sets a date — and at that point Todoist also **dropped the Today option** (the list became Tomorrow · Next week · Next weekend · No Date), so the quick option matching the current date disappears. meologue showed "No Date" absent on one opening and present on another, but neither artifact records which task each opening was on, so meologue's half is not established; whether meologue hides the option matching the current date is an open question for the next pass. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-content-meologue.json`, `flow3-SCHED-03-meologue.json` |
| SCHED-04 | A "Type a date" input accepts dates and recurrence phrases and previews the resolution above the quick options | scheduler-and-priority.md §3 | `task-schedule-popover.tsx`'s `resolveSchedulePreview`, reusing `@meologue/core`'s `parseQuickAdd`/`firstOccurrence` | `matched` | **Driven live on both sides with identical phrases (flow 3):** `next friday` → Fri 25 Sep, `every monday` → Mon 14 Sep → Forever, `in 3 days` → Tue 15 Sep, in both. The task-count suffix differed only because meologue's fixtures sat on 15 Sep. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-04-meologue-raw.json`. Preview shows the resolved date and a real Task count (`datesWithTasks`, threaded from the caller's own task list) — not a fabricated "No tasks" placeholder. The "→ Forever"/"→ Ends" suffix is unverified beyond the unbounded case: a bounded phrase's own resolved end date is not re-derived (would require exposing `../recurrence/engine.ts`'s private `resolveBoundDate`), so it renders the generic "→ Ends" rather than a specific date |
| SCHED-05 | Picking a date **writes the phrase back into the title** as a highlighted recognition token | scheduler-and-priority.md §3 | — | `divergent` | Deliberately not reproduced, and re-statused from `todoist-captured` because "captured, nothing built" misdescribes a row nobody intends to build: that behaviour is Todoist's Quick Add typing a still-unsaved title at the same time a date is picked. This popover schedules an already-created Task with no title being composed alongside it, so there is no title to write into. The *parsing* mechanism is still shared (SCHED-04's own reuse of `todo-quick-add-recognition.ts`'s underlying `parseQuickAdd`/`firstOccurrence`), which is the part of this row that actually applies here — the DOM side-effect does not |
| SCHED-06 | Calendar week starts **Monday** | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `<Calendar weekStartsOn={1}>`, `formatters.formatWeekdayName` | `matched` | Header reads `M T W T F S S`, verified in-app. **Driven live on both sides (flow 3):** identical. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-content-meologue.json` |
| SCHED-07 | Today carries **no `aria-current`** and no ring — only bold and `rgb(226,106,96)` | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `classNames.today`; `--td-calendar-today` | `divergent` | **A real defect that only shows when today is a weekend — driven live on both sides on Saturday 12 Sep (flow 3).** Todoist's today cell carries its weekend (`calendar__day--greyed`) and busy classes and still renders today-red `rgb(226,106,96)`, bold. meologue's renders **grey `rgb(204,204,204)`**, bold: the cell receives both `[&>button]:text-[color:var(--td-calendar-today)]` and the weekend `[&>button]:text-muted-foreground`, and the weekend utility wins the cascade. The capture this row came from was a Thursday, so the collision was never visible. Neither app sets `aria-current`, which still holds. Fix candidate. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-06-07-08-09-10-meologue.json`. **Earlier note:** Deliberate; do not "fix". Verified in-app: today's cell carries no `aria-current`, bold text, `rgb(226,106,96)`. **This row's own test was true on exactly one day.** It asserted `data-today="true"` on 2026-09-10 — the capture instant — through an attribute react-day-picker derived from the *real* system clock, while everything else in the popover derives from the injected `now`. The suite went red the morning after the capture and would have stayed red permanently; `today={now}` now threads the same clock through DayPicker. A date-pinned test that reads one value off the wall clock is pinned in name only |
| SCHED-08 | Selected day is a filled circle, `rgb(222,76,74)`, 24px, radius 12px | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `classNames.selected`/`day_button` (`size-6 rounded-full`); `--td-calendar-selected` | `matched` | Verified in-app. **Driven live on both sides (flow 3):** a filled `rgb(222,76,74)` 24×24 circle with bold white text, in both. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-08-meologue.json` |
| SCHED-09 | Days carrying tasks get a 3×3px dot via `::before`, no extra DOM node | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `modifiersClassNames.busy`; `--td-calendar-busy-dot` | `matched` | Verified in-app against real seeded Tasks. **Driven live on both sides (flow 3):** a `::before` dot, 3×3px, `rgb(209,209,209)`, absolutely positioned, with no extra DOM node, in both. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-06-07-08-09-10-meologue.json` |
| SCHED-10 | Weekends dim to `rgb(204,204,204)`, independent of today/busy | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `modifiersClassNames.weekend` (reuses `--muted-foreground`, which already resolves to this exact value under this surface's dark theme) | `matched` | **Driven live on both sides, same session (flow 3):** a weekend day with no other state renders `rgb(204,204,204)` at weight 400, a weekday `rgb(255,255,255)` at 400, in both. The nuance's "independent of today" does **not** hold when today is itself a weekend — Todoist keeps today red there, and meologue does not; that collision is SCHED-07's defect, not this row's. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-SCHED-06-07-08-09-10-meologue.json` |
| SCHED-11 | Time opens a second dialog with start time, duration and timezone, defaulting to "Floating time" | scheduler-and-priority.md §5; re-read live (flow 3): `role="dialog"` `aria-label="Select start and end time"`, 306×216, radius 10px, `rgb(40,40,40)`, fields Start time · Duration ("No duration") · Time zone ("floating"), buttons Save · Cancel (`live-audit-dom/flow3-SCHED-todoist.json`) | `task-schedule-popover.tsx:444-449`: an inline **"Add a time"** checkbox revealing an `<input type="time">` inside the popover — read from source, not driven live | `divergent` | **Compared, and the shape differs:** Todoist opens a separate time dialog carrying start time, a Pro-gated duration and a floating time zone; meologue has an inline checkbox and time input with no duration (removed, SCHED-12) and no zone control (floating by the glossary rule). The flow-3 run reported "no equivalent Time control exists anywhere in meologue" — that came from a case-sensitive search for `Time`, which cannot match "Add a time", and is not recorded. Matches our floating-time glossary rule. Out of this ticket's scope — the existing "Add a time" toggle/`<input type="time">`, in `task-schedule-popover.tsx` since issue #249 (this row's own citation was already stale — #253 confirms the toggle has no presence in `task-schedule-sheet.tsx` at all any more, that file's Date section having been removed outright), is untouched, not the dedicated dialog described here |
| SCHED-12 | Duration is Pro-gated | scheduler-and-priority.md §5 | — | `divergent` | We removed Duration deliberately (issue #179) |
| SCHED-13 | The Deadline picker's own UI | — | — | `blocked` | **Pro-only paywall** |
| SCHED-14 | The dedicated Repeat dialog's contents | pass2-2026-09-11.md §6 — `role="menu"`, 282×206, radius 10px, `rgb(40,40,40)`, `1px solid rgb(61,61,61)`, `rgba(0,0,0,.12) 0 0 8px`, z-index 1000; items in order: Every day, Every week on Friday, Every weekday (Mon–Fri), Every month on the 11th, Every year on September 11th, Custom… | — | `todoist-captured` | Never previously opened; now fully captured. Still unbuilt — meologue has no dedicated Repeat dialog, only the recurrence phrase typed into the scheduler's "Type a date" input (SCHED-04). **Re-read live on Saturday 12 Sep (flow 3):** `role="menu"`, 282×206, radius 10px, `rgb(40,40,40)`, `1px solid rgb(61,61,61)`; items Every day · Every week on Saturday · Every weekday (Mon - Fri) · Every month on the 12th · Every year on September 12th · Custom… — the options track the current date ("on Friday" in pass 2, "on Saturday" now). Artifact: `live-audit-dom/flow3-SCHED-todoist.json` |
| PRI-01 | Picker swatches: P1 `rgb(209,69,59)`, P2 `rgb(235,137,9)`, P3 `rgb(36,111,224)`, P4 `rgb(102,102,102)` | scheduler-and-priority.md §10a | All four literals exist as `--td-priority-picker-*` tokens (`index.css`), mapped by `lib/task-priority-colors.ts` `priorityPickerColour` and painted as the picker's swatches in `task-command-menu.tsx` | `matched` | **Status was stale** — landed with #223's palette and never reflected here. Four confirmed genuinely distinct. See PRI-03 for the one swatch whose rendering is contested. **Todoist's picker re-read live (flow 2):** P1 `rgb(209,69,59)`, P2 `rgb(235,137,9)`, P3 `rgb(36,111,224)`, P4 `rgb(102,102,102)` — unchanged. meologue's picker was not driven live (only its tokens were read), so this stays `built`. Artifact: `live-audit-dom/flow2-PRI-01-02-03picker-04-todoist.json`. **Now `matched`: both pickers read in the same session (flow 3).** Painted swatches P1 `rgb(209,69,59)`, P2 `rgb(235,137,9)`, P3 `rgb(36,111,224)`, P4 `rgb(102,102,102)`, identical in both. The pickers' *structure* differs — see PRI-02. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-PRI-01-02-03-meologue.json` |
| PRI-02 | Internal `data-value` is **inverted** from the label — P1 is 4 | scheduler-and-priority.md §10a; re-read live (flow 2): Priority 1 → `data-value="4"`, Priority 2 → `"3"`, Priority 3 → `"2"`, Priority 4 → `"1"` | live flow 3: `role="menu"` with four `role="menuitem"` rows labelled P1–P4, state on `aria-pressed`, **no `data-value` attribute** (`live-audit-dom/flow3-PRI-01-02-03-meologue.json`) | `divergent` | Our store already inverts identically — but the picker's DOM does not expose it. **Compared live, same session (flow 3):** Todoist's picker is a listbox of options labelled "Priority 1"…"Priority 4", each carrying the inverted `data-value` (4…1) and `aria-selected`; meologue's is a Radix menu of items labelled "P1"…"P4" with `aria-pressed` and no `data-value`. Structural, not user-visible; the inversion itself is correct in meologue's store. A fix only if DOM parity is wanted. Artifacts: `live-audit-dom/flow3-SCHED-todoist.json`, `flow3-PRI-01-02-03-meologue.json` |
| PRI-03 | P4 is pre-selected and is the default; its row ring is **1px opaque grey `rgb(169,169,169)`** and its picker swatch a different grey, `rgb(102,102,102)` — **not** transparent | scheduler-and-priority.md §10d | "Pre-selected" is built: an untouched Task stores `priority: 1` → ui 4, and `task-command-menu.tsx` marks it `aria-pressed`. The **transparent** ring is not: the P4 swatch paints `--td-priority-picker-4` = `rgb(102,102,102)`, and a P4 row's checkbox paints `--td-checkbox-ring-default` = `rgb(169,169,169)` | `matched` | **Unblocked — settled live (flow 2), and matched once both pickers were read together (flow 3):** P4 is pre-selected for an untouched task in both (`aria-selected="true"` in Todoist, `aria-pressed="true"` in meologue) and its swatch is `rgb(102,102,102)` in both (`flow3-SCHED-todoist.json`, `flow3-PRI-01-02-03-meologue.json`). The paragraph below is flow 2's reading. The two corpus documents were not describing different surfaces; one was wrong. Todoist's P4 row ring is `1px solid rgb(169,169,169)` and its picker swatch `rgb(102,102,102)`, with P4 pre-selected in the picker (`aria-selected="true"`). `scheduler-and-priority.md` §10c/§10d's "fully transparent ring" does not reproduce and is corrected there; `row-and-detail.md` §1's grey was right. meologue's P4 row ring is the same `1px rgb(169,169,169)`, read live. At that point it stayed `built` only because meologue's picker had not been driven live; flow 3 closed that. Artifacts: `live-audit-dom/flow2-PRI-03-tiebreak-ROW-03-PRI-05-06-todoist.json`, `flow2-ROW-03-PRI-05-06-PRI-03-meologue.json`, `flow2-PRI-01-02-03picker-04-todoist.json`. **Previously `blocked`:** the two documents disagreed (transparent vs `rgb(169,169,169)`) and the row could not be closed until a capture settled it |
| PRI-04 | The Quick Add pill colours only the **flag icon**; its `P1` text stays neutral grey | scheduler-and-priority.md §10b | — | `todoist-captured` | |
| PRI-05 | The row checkbox ring red `rgb(255,112,102)` is **not** the picker red `rgb(209,69,59)` | scheduler-and-priority.md §10c | `task-priority-colors.ts` `priorityColour` (`--td-priority-row-1`) vs `priorityPickerColour` (`--td-priority-picker-1`) | `divergent` | Two surfaces, two values — don't unify into one token. Implemented by issue #223, before this ledger row was ever pointed at code; issue #224 only updated this row to say so. **Driven live (flow 2): the rule holds at every level in Todoist, but only at P1 in meologue.** Todoist row vs picker: P1 `rgb(255,112,102)` vs `rgb(209,69,59)`; P2 `rgb(255,154,19)` vs `rgb(235,137,9)`; P3 `rgb(82,151,255)` vs `rgb(36,111,224)`. meologue's `--td-priority-row-2` and `-3` hold the picker values (`#eb8909`, `#246fe0`) — exactly the unification this row warns against, for P2 and P3. Fix candidate, together with ROW-03 and PRI-06 |
| PRI-06 | Row rendering for P2 and P3 | live flow 2 (`live-audit-2026-09-11.md`): P2 ring `2px rgb(255,154,19)`, P3 ring `2px rgb(82,151,255)` — supersedes scheduler-and-priority.md §10c's inferred values | live flow 2: P2 `1px rgb(235,137,9)`, P3 `1px rgb(36,111,224)` | `divergent` | **Unblocked — P2 and P3 measured for the first time**, using disposable fixtures created in Todoist and deleted afterwards. Both axes differ; see ROW-03 and PRI-05. Previously `blocked`: no P2/P3 task existed in the account. Artifacts: `live-audit-dom/flow2-PRI-03-tiebreak-ROW-03-PRI-05-06-todoist.json`, `flow2-ROW-03-PRI-05-06-PRI-03-meologue.json` |
| ROW-01 | Row height is 59px for a row carrying one metadata line; a **title-only row is 43px** in Todoist. Zero padding on the row itself | row-and-detail.md §1; live flow 2 (`live-audit-2026-09-11.md`): dated rows 59px, title-only `ZZ probe P1` **43px** | live: 59px on `ZZ probe P1`, which carries a `P1` text badge (`task-row-content.tsx:522`); source: a `minHeight: "59px"` floor (`task-row-content.tsx:349`), so a title-only row cannot drop below 59 | `divergent` | **Restatused from `matched`.** The corpus's 59px was measured on "hair wash", which carries a date badge — so it was always the one-metadata-line height, never a fixed one, and "matched" was true only for that case. Todoist renders a title-only row at **43px** (+16px for a metadata line); meologue's 59px floor holds every title-only row at 59. Fix candidate. Honest limit: a like-for-like title-only meologue row was not measured live (the P1 fixture carries a priority badge), so meologue's side of the 43px case is read from source. Artifacts: `live-audit-dom/flow2-ROW-01-02-todoist.json`, `flow2-ROW-01-02-meologue.json` |
| ROW-02 | Divider is `border-bottom: 1px solid rgb(61,61,61)` with **0px left inset** — full list width | row-and-detail.md §1 | measured in-app: `1px solid rgb(61,61,61)` on the row box | `matched` | First probe read 0px — it measured the `<li>` where the divider sits on the row box. The code was right and the probe was wrong. **Re-read live on both sides, same session (flow 2):** `1px solid rgb(61,61,61)` in both. The 0px left inset was not re-compared that session. Artifacts: `live-audit-dom/flow2-ROW-01-02-todoist.json`, `flow2-ROW-01-02-meologue.json` |
| ROW-03 | Checkbox: 24×24 hit box around an 18×18 visible ring, `1px solid rgb(169,169,169)` (2px at P1), tinted by priority | row-and-detail.md §1; pass2-2026-09-11.md §2 (ring width: P1 2px, P4 1px) | measured in-app: 18x18 in a 24x24 hit box; ring `rgb(169,169,169)`, `rgb(255,112,102)` at P1; `task-row-content.tsx:330-362`. **Issue #250 fixed the width axis**: the `box-shadow` spread is now `uiPriorityOf(task.priority) === 1 ? "2px" : "1px"` (`task-row-content.tsx`, the checkbox `<input>`'s own `style`), no longer a flat `1px` for every priority | `divergent` | **Driven live with P1–P4 fixtures in both apps (flow 2) — P2 and P3 are wrong on both axes.** Todoist: P1 `2px rgb(255,112,102)`, P2 `2px rgb(255,154,19)`, P3 `2px rgb(82,151,255)`, P4 `1px rgb(169,169,169)`. meologue: P1 and P4 identical; P2 `1px rgb(235,137,9)`, P3 `1px rgb(36,111,224)`. So the ring is 2px for **every non-default priority**, not only P1 — `task-row-content.tsx:428` tests `uiPriorityOf(...) === 1` — and P2/P3 paint the *picker's* colours, because `scheduler-and-priority.md` §10c recorded them as "INFERRED only (not verified) from the picker's flag colours" and that inference was built. Fix candidate on both axes. Hit box 24×24 and visible ring 18×18 re-confirmed in meologue. Artifacts: `live-audit-dom/flow2-PRI-03-tiebreak-ROW-03-PRI-05-06-todoist.json`, `flow2-ROW-03-PRI-05-06-PRI-03-meologue.json`. **Earlier note, kept for its structural half:** **Still not `matched` — one axis fixed, one still open.** This row's own "What `matched` requires" section demands behavioural, structural AND dimensional agreement. Dimensionally, ring width now varies by priority as measured (`task-row.test.tsx`'s own P1/non-P1 tests pin it) — real-browser pixel confirmation is still outstanding, jsdom reads the inline style but computes no cascade. Structurally, the divergence this row already carried is untouched by issue #250 (styling-only): Todoist uses `<button class="task_checkbox" role="checkbox" aria-label="Mark task as complete">`; meologue uses `<label><input type="checkbox" readOnly aria-label={task.content}>` — different element, different role, different accessible name. **Caveat carried forward:** the `rgb(169,169,169)` ring value itself is contradicted by `scheduler-and-priority.md` §10c/§10d, which measured a *transparent* ring for P4 — see PRI-03, `blocked` on exactly that |
| ROW-04 | Hover reveals drag handle, then Edit · Date · Comment · More, each `opacity` 0→1; the row background does **not** change | row-and-detail.md §1 | measured in-app: every affordance `opacity: 0` at rest | `matched` | Initially failed: variant classes built by string interpolation, which Tailwind's text scanner never sees, so no CSS existed and every control showed on every row |
| ROW-05 | Long titles **wrap** to 4 lines via `-webkit-line-clamp: 4`, they do not truncate to one | row-and-detail.md §1 | measured in-app: `-webkit-line-clamp: 4`, title 14px/21px | `matched` | **Driven live on both sides, same session (flow 2):** `-webkit-line-clamp: 4` and 14px/21px in both. Artifacts: `live-audit-dom/flow2-ROW-05-todoist.json`, `flow2-ROW-05-meologue.json` |
| ROW-06 | Markdown in a **title** stays literal | row-and-detail.md §1 | `task-row-content.tsx`: `task.content` interpolated as plain text, never parsed | `built` | |
| ROW-07 | A Description renders as **real HTML from markdown**, previewed as a first line beneath the title | row-and-detail.md §1 | `task-row-content.tsx`: first `\n`-delimited line of `task.description`, rendered via `inlineProse`, truncated to one line | `matched` | **Driven live on both sides (flow 2)** with description `first line **bold**` plus a second line: both render the first line as real HTML (`<strong>`) and show exactly one line. The mechanism differs with no visible effect — Todoist clamps a div holding both `<p>`s with `-webkit-line-clamp: 1`, meologue renders only the first line with `truncate`. Inline markdown only (bold/italic/code/links) via `inlineProse`, not the block-level `entryProse` (lists, multiple paragraphs) — a first-line preview by definition can't show a second block anyway. Artifacts: `live-audit-dom/flow2-ROW-06-07-08-todoist.json`, `flow2-ROW-06-07-meologue.json` |
| ROW-08 | Comment count is an icon plus number, linking to `?intent=reply` | row-and-detail.md §1 | `task-row-content.tsx`: `MessageSquare` icon + count | `divergent` | **Compared live on both sides (flow 2):** Todoist's badge is `<a aria-label="2 comments" href="/app/task/…?intent=reply">`; meologue's is a plain `<span>` holding a `MessageSquare` icon and the count. Artifacts: `live-audit-dom/flow2-ROW-06-07-08-todoist.json`, `flow2-ROW-08-09-10-meologue.json`. **Divergent from the reference in one respect, not silently**: this is a plain badge, not an `<a href="…?intent=reply">` — clicking it does nothing on its own (the separate Comment hover action opens the detail view instead). Not touched by issue #224; carried over unchanged from issue #180 |
| ROW-09 | Metadata blocks are adjacent flex children with a gap — no separator glyph between date and comments | row-and-detail.md §1 | `task-row-content.tsx`: one `flex flex-wrap items-center gap-x-2` line for every badge, old and new alike | `built` | Extended to the newly-added Labels/Project/sub-task-count badges under the identical rule — see ROW-10 for why their very presence/order is still unverified. **meologue re-read live (flow 2):** comments, two Labels and the sub-task count each sit in the one `flex flex-wrap gap-x-2` line with no separator glyph. Todoist's separator was not re-read that session, so this row stays `built`. Artifact: `live-audit-dom/flow2-ROW-08-09-10-meologue.json` |
| ROW-10 | Labels, the Project a Task lives in, and a sub-task count all render on the row, as further adjacent flex children with no separator | — | `task-row-content.tsx`: `LabelBadge` per resolved Label, Project name via `projectNameFor`, sub-task count via `ListTree` + number | `divergent` | **Compared live (flow 2): two real differences.** (1) **Priority**: Todoist's `task-info-tags` for a P1 task is *empty* — priority shows only as the checkbox ring — while meologue renders a `P1`/`P2`/`P3` text badge for every non-default priority (`task-row-content.tsx:522`; read from source, because the flow-2 summary asserted it but its meologue artifact never measured it). (2) **Labels**: Todoist's badge is a real `<a href="/app/label/…">`; meologue's is a plain `<span>` with a colour dot. **Still unestablished:** whether Todoist shows a Project badge (not inside a project's own view — a cross-project view is needed) or a sub-task count (virtualization scrolled the parent row out before it was read). Artifacts: `live-audit-dom/flow2-ROW-09-10-todoist.json`, `flow2-ROW-08-09-10-meologue.json`. **Earlier note:** **Unverified.** ROW-09's own note already conceded the gap: none of the 7 captured Inbox tasks carried a Label, a cross-Project view, or a sub-task, so neither their presence, their order relative to Date/Priority/comments, nor any icon choice for them was ever observed. This row exists so that gap has a name rather than being silently folded into ROW-09's "matched" claim |
| ROW-11 | Hovering a row mounts Complete · Edit · Date · Comment · More, in that order; clicking the **Edit** pencil activates the shared `tiptap ProseMirror` editor in place — no dialog, no URL change | driven directly, issue #225's own follow-up | driven in-app: Edit pencil opens `contenteditable role=textbox aria-label="Task name" class="ProseMirror tiptap"`, URL unchanged, rename persisted | `matched` | Matched on gesture, markup and URL behaviour. One structural divergence kept: Todoist's editor sits OUTSIDE the `li` (it swaps the row for an edit form); ours renders inside it. The double-click gesture first built here was an invention and was replaced once the pencil was actually driven in Todoist |
| ROW-12 | The row's hover controls (Edit/Date/Comment/More) are **not in the DOM at rest at all** — Todoist mounts them on hover rather than rendering them permanently | driven directly, issue #225's own follow-up | `task-row-content.tsx`: all four rendered unconditionally, revealed via `opacity-0`/`group-hover:opacity-100` (ROW-04's own `matched` measurement, which read the computed style but not whether the elements existed at rest) | `divergent` | Deliberate accessibility choice, not an oversight: permanently-mounted controls stay reachable by keyboard `Tab`/focus without a hover event ever firing, which a reader with no pointer needs. Mount-on-hover would make them unreachable by keyboard alone unless a second, hover-independent path were built — not worth trading away for DOM parity with no behavioural upside. |
| ROW-13 | Today's Due-today rows render a redundant date badge; Todoist omits the date control from the DOM entirely on such a row, rather than hiding it | pass2-2026-09-11.md §3 ("Today view omits the date control entirely — the control is absent from the DOM, not hidden") | **Issue #250 built the suppression path.** `TaskRowContent` gained a `suppressDateBadge` prop (`task-row-content.tsx`), threaded through `TaskRow` and set unconditionally on every row in `today-view.tsx`'s own `dueToday` section (its Overdue section leaves it unset — an overdue row's date is never redundant); when set, the date badge is omitted from the render entirely, not merely hidden with CSS, matching the DOM-absence pass2 measured | `matched` | **Driven live on both sides, same session, on each app's Today view (flow 2):** a due-today row carries no date control in the DOM in either, while an overdue row keeps its date. One sub-case differs and is recorded under DATE-04 — a *recurring* task due today. Artifacts: `live-audit-dom/flow2-ROW-13-todoist.json`, `flow2-ROW-13-meologue.json`. `today-view.test.tsx`'s own test pins the DOM absence side by side with an overdue row that still renders its date. Not `matched`: nobody has driven the two apps side by side in a real browser — this is a jsdom/RTL confirmation that the element is absent from the tree, the same axis pass2 measured, but not a live comparison |
| ROW-14 | The main task list segregates completed Tasks into a collapsed `<details>` below the active list; Todoist interleaves completed rows inline within the same list | pass2-2026-09-11.md §3 (measured: Inbox contains 7 active + 6 completed = 13 `li[data-item-id]` in one list, each `aria-checked`) | `completed-tasks.tsx` renders a separate collapsed `<details>` (`:36-61`); `task-row-content.tsx:333` hardcodes `checked={false} readOnly` on the active row's own checkbox | `divergent` | **Compared live (flow 2):** Todoist keeps a completed row in the same list as active rows, with `aria-checked="true"` and a `--completed` class; meologue segregates it into a collapsed `<details><summary>Completed (1)`. Artifacts: `live-audit-dom/flow2-ROW-14-15-DATE-02-todoist.json`, `flow2-ROW-14-15-DATE-02-meologue.json`. New row — structural, not cosmetic (audit-2026-09-11.md §4 item 6): the active row's checkbox can never render checked, so that list shape structurally cannot hold a completed row even before considering where it's placed. **Issue #250 (the three cheap, styling-only fixes ROW-15/ROW-13/ROW-03 above) deliberately left this row untouched** — its own acceptance criteria never asked for the structural merge, and closing it means rebuilding the active/completed list as one interleaved, `aria-checked` collection rather than a styling change to either half. Recorded here explicitly so the deferral reads as a decision, not an oversight |
| ROW-15 | A completed row's title and date carry two distinct colours, and the title always strikes through; meologue paints only the title muted and gates strikethrough behind a Settings default | pass2-2026-09-11.md §1 (completed title `rgb(128,128,128)` + `line-through`; completed date `rgb(204,204,204)`) | **Issue #250 fixed both axes.** `index.css`'s `[data-surface="todo"]` scope now re-points `--checked-list-text-decoration`/`-color` unconditionally (`line-through` / `rgb(128,128,128)`), overriding whichever of the four `data-completed-style` values `<html>` carries — the same "RE-POINT, DON'T RENAME" move this scope already makes for `--background` et al., so only Todo's own completed rows (`.completed-task-text` consumers: `completed-tasks.tsx`, `task-detail-view.tsx`, `filter-view.tsx`, `task-search-page.tsx`) always strike through; History/Composer, outside this scope, keep obeying the Settings option unchanged. `completed-tasks.tsx` also now renders the Task's own date beside the title (previously dropped entirely), through `formatTaskDate({ completed: true })` — DATE-02's existing "completion overrides tone" rule already resolves it to `--td-date-muted` = `--muted-foreground` = `rgb(204,204,204)` in this scope's Dark block, with no new token needed for that half | `matched` | **Driven live on both sides, same session (flow 2):** completed title `rgb(128,128,128)` with `line-through`, completed date `rgb(204,204,204)`, identical in both — the real-browser confirmation this note called outstanding. Artifacts: `live-audit-dom/flow2-ROW-14-15-DATE-02-todoist.json`, `flow2-ROW-14-15-DATE-02-meologue.json`. `completed-tasks.test.tsx` pins the date now rendering (and staying absent when a Task has none); the strikethrough/colour override is a CSS re-point jsdom's own lack of a cascade can't pin from a test, the same limit `format-task-date.test.ts`'s own colour assertions already accept for every other `--td-*` token. Not `matched`: real-browser confirmation of the computed `rgb(128,128,128)` + `line-through` is still outstanding |
| DET-01 | Opening a task changes the URL to `/app/task/{slug}-{id}`; Escape returns | row-and-detail.md §2 | `lib/task-detail-route.ts` `taskDetailPath`/`taskIdFromParam`, routed in `App.tsx`; Escape reaches `closeTaskDetail()` because the Radix `Dialog.Content` does not override `onEscapeKeyDown` | `matched` | **Driven live on both sides, same session (flow 4):** opening a task changes the URL to `/app/task/zz-probe-detail-<id>` in Todoist and `/todo/task/zz-probe-detail-<id>` in meologue — same slug-plus-id shape — and Escape returns to the Inbox URL in both. Artifacts: `live-audit-dom/flow4-DET-01-open-todoist.json`, `flow4-DET-01-meologue.json`. **Status was stale** — the route and the Escape path both predate this ledger. One deliberate divergence: our prefix is `/todo/task/…`, not Todoist's `/app/task/…`, because Todo is a Destination inside this app rather than the app itself (that module's own header comment) |
| DET-02 | The detail title at rest is a **non-editable `div.task_content`** — the same display component the row uses, not Quick Add's editor | row-and-detail.md §2 | `task-detail-view.tsx` (`editingTitle`) | `divergent` | Display and edit are split. A `<button>`, not a bare `<div>`, is meologue's own choice for how "activate" is reached without a pointer — the reference never had to specify since only a click was driven. **Compared live (flow 4):** Todoist's title at rest is `div.task_content` with `tabIndex=-1`, not contenteditable; meologue's is a `<button>`, not contenteditable. Both are non-editable displays that swap to an editor on activation, which is the behaviour this row names; the element differs, for the keyboard-reachability reason above. Artifacts: `live-audit-dom/flow4-DET-02-03-05-14-todoist.json`, `flow4-DET-02-03-05-14-meologue.json` |
| DET-03 | The title carries the hint "Activate to edit the task name" | row-and-detail.md §2 | `task-detail-view.tsx` (`titleHintId`, `aria-describedby`) | `divergent` | Verbatim text, on a visually-hidden `<span>` the display button's own `aria-describedby` points to. **Compared live (flow 4) — same text, different wiring, and meologue's is the more accessible.** Both render the hint "Activate to edit the task name" visually hidden. In Todoist it sits in a sibling element (`id="a11y_task_name"`) that **nothing references** — the title carries no `aria-describedby`, so a screen reader is not told the hint belongs to the title. meologue's title points at its hint through `aria-describedby`. Matching Todoist exactly would mean removing that link and making meologue less accessible, so this is a **decision for the user**, not a fix candidate. Artifacts: `live-audit-dom/flow4-DET-02-03-05-14-todoist.json`, `flow4-DET-02-03-05-14-meologue.json` |
| DET-04 | What the title becomes **once activated** for editing | live flow 4, on a disposable task: `div[role="textbox"][contenteditable="true"][aria-label="Task name"]`, class `tiptap ProseMirror`, with Cancel and Save buttons appearing (`live-audit-dom/flow4-DET-04-06-09-todoist.json`) | live flow 4: the identical element — `div[role="textbox"][contenteditable="true"][aria-label="Task name"]`, class `ProseMirror tiptap`, with Cancel and Save (`flow4-DET-04-06-09-meologue.json`) | `matched` | **Unblocked** by a disposable task created and deleted in the same run, so no real task was typed into. Previously `blocked`: observing it would have meant editing a real task |
| DET-05 | The detail dialog is `data-testid="task-details-modal"` | keyboard.md §1 | `task-detail-view.tsx:967` | `matched` | **Driven live on both sides (flow 4):** the detail dialog carries `data-testid="task-details-modal"` in both. Artifacts: `live-audit-dom/flow4-DET-02-03-05-14-todoist.json`, `flow4-DET-02-03-05-14-meologue.json`. **Rot corrected** — this row read "nothing built" while `task-detail-view.tsx:967` already carries `data-testid="task-details-modal"` and cites DET-05 by name in its own comment |
| KBD-01 | The overlay lists **80 shortcuts across 8 sections**: General 13, Quick Add 11, Navigate 15, Edit task 17, Add task 5, Sub-task 3, Projects 11, Calendar/Upcoming 5 | keyboard.md §1 | — | `todoist-captured` | ~~Transcribed, not driven~~ **Now driven (flow 6), and the transcription holds exactly.** Pressing `?` opened a `role="dialog"` titled "Keyboard Shortcuts" with eight section headings in order — General 13 · Quick Add 11 · Navigate 15 · Edit task 17 · Add task 5 · Sub-task 3 · Projects 11 · Calendar and Upcoming views 5 — **totalling 80**, matching `keyboard.md`'s transcribed table. Method caveat, recorded because it matters: the per-section count came from a content-hashed class selector, and a second selector in the same run produced different per-section numbers; the 80 total is what agrees with the transcription. meologue's own overlay is a deliberate subset (KBD-02) and is not compared here. Artifacts: `live-audit-dom/flow6-KBD-01-02-todoist.json`, `flow6-KBD-01-rowcounts-todoist.json`, `flow6-KBD-01-structure-debug.json` |
| KBD-02 | `?` opens the shortcuts overlay | keyboard.md §1 | `todo-keyboard-shortcuts-overlay.tsx`, wired by `use-todo-keymap.ts` | `built` | Issue #228. Not `matched` — nobody drove Todoist and meologue side by side for this row, only meologue itself (this ticket's own browser verification). Our overlay renders far fewer than 80 rows: only bindings `todo-keymap.ts` actually wires (that module's own header comment lists what's deliberately absent and why). |
| KBD-03 | **Both** arrow keys and `j`/`k` move focus between rows | keyboard.md §2 | — | `todoist-captured` | Verified via `document.activeElement`. Not touched by issue #228 — row-to-row focus movement is a separate, larger feature (this app's rows currently reach each other by `Tab` order alone) than the single-Task/navigation bindings this ticket wires. |
| KBD-04 | Focus **wraps** at both ends, and the cycle includes the "Add task" button and completed rows | keyboard.md §2 | — | `todoist-captured` | Verified |
| KBD-05 | The focus ring is an **inset box-shadow** `rgb(23,91,194) 0 0 0 1px` plus a `rgba(255,255,255,0.14)` background tint — the CSS `outline` property is unset | keyboard.md §2 | — | `todoist-captured` | Verified in Todoist. Unbuilt here: `--td-focus-ring` is declared in `index.css` with **zero consumers**, and focus is still drawn by Tailwind's `focus-visible:ring-*`/`outline` utilities — so this row's "`outline` is unset" clause is currently contradicted by our own CSS. Third instance of the dead-token trap; see QA-16 and QA-20 |
| KBD-06 | The full 80-key map, key by key | keyboard.md §1 | `@/lib/todo-keymap.ts` (`TODO_KEY_BINDINGS`) | `built` | Issue #228 wires a small, named subset — Quick find (`/`, `f`, `⌘K`), `?`, `.` (More actions), `⌘E` (Edit), `T`/`⇧T` (Date/Remove date), `D`/`⇧D` (Deadline/Remove deadline), `Y` (Priority), `⌘⌫`/`⇧Delete` (Delete), `⇧G` (open Task in its Project), and `G`-then-`I`/`T`/`U`/`P`/`V` (go to Inbox/Today/Upcoming/Projects/Filters). **Issue #253 split `T` off from `D`/`Y`:** `D`/`Y` still open the combined schedule sheet (Deadline/Priority, `task-schedule-sheet.tsx`), but `T` now opens the row's own anchored `TaskSchedulePopover` instance via `OPEN_SCHEDULE_EVENT` (todo-keymap.ts) instead — this row's own former wording ("Y … opens the same combined schedule sheet as T/D") is corrected here rather than left to misdescribe the binding table it cites. Every other key stays `todoist-captured`, unimplemented. **Overclaim corrected:** this note used to say `todo-keymap.ts`'s own header comment "names each key left out and why." It does not — `Z`/`⌘Z` (undo), `⌘A`, the arrow keys, `Esc` and `⌘⌥0` are all absent from `TODO_KEY_BINDINGS` (`todo-keymap.ts:95-216`) with no acknowledgement anywhere in the file. That silence is why `CMT-05`'s missing undo binding went unflagged for as long as it did. `V` ("Move to…") specifically was left out even though a menu item already invites it: no controlled way exists to open that one Radix submenu pre-expanded without a larger refactor. Not `matched` for the same reason as KBD-02 — meologue-only verification, not a side-by-side drive. |
| DET-06 | Activating the title swaps in `tiptap ProseMirror` editors labelled "Task name" and "Description" — **the same component as Quick Add** | lifecycle.md, quick-add.md | driven in-app: same editor markup in the row's inline rename; detail view uses the identical component | `matched` | Matched for row and detail. **Not yet in [meologue's own] Quick Add** — it still runs the older backdrop highlighting, deliberately, until #226 lands a decoration plugin; converting it early would delete working recognition for a whole ticket. **Re-driven live on both sides (flow 4):** activating the detail title shows both `aria-label="Task name"` and `aria-label="Description"` `tiptap ProseMirror` editors at once, in both. (The "not yet in Quick Add" caveat above is stale — #226 converted Quick Add, see QA-17.) Artifacts: `live-audit-dom/flow4-DET-04-06-09-todoist.json`, `flow4-DET-04-06-09-meologue.json` |
| DET-07 | Recognition fires in the **detail** title exactly as in Quick Add — same span, padding and match id — and a recognised phrase resolves on save, exactly as it does for a new Task | lifecycle.md; `rename-capture-2026-09-11.md` (both of Todoist's own rename surfaces driven directly) | `task-detail-view.tsx:367-391,578-580` (highlighting), `task-title-commit.ts`'s `commitTaskTitle` (resolving), `todo-page.tsx`'s and `composer-page.tsx`'s own `commitRename` wrappers, `task-row-content.tsx:387-394` (the inline row editor, which gained the identical highlighting plugin the same ticket) | `matched` | **Driven live on both rename surfaces' detail view, same session (flow 4).** Typing ` tom` into the activated detail title produced `span[data-testid="natural-language-match"][data-highlighted-match="true"]`, `display: inline-block`, padding 0 4px, in both — `data-match-id` `"13 Sep"` in Todoist against `"2026-09-13"` in meologue, the format difference QA-02 already records. On Save both stored the title as `ZZ probe rename` with the phrase stripped and the Date set to Tomorrow. The toast Todoist raises on that save is a separate row, DET-16. Artifacts: `live-audit-dom/flow4-DET-07-08-presave-todoist.json`, `flow4-DET-07-08-postsave-todoist.json`, `flow4-DET-07-08-presave-meologue.json`, `flow4-DET-07-08-postsave-meologue.json`. **Restatused (issue #247), and the reference is no longer silent.** Highlighting was already built and cited here (#225/#226). Saving was not: until now, `saveEditing`/`commitTitle` both handed the raw typed text straight to `onRename`, which reached `renameTask` directly — a phrase rendered a span but never touched a Task field, and the row editor had no recognition plugin attached at all. #247 moved `onRename` one layer up on both surfaces (the page's own `commitRename`) so it resolves through the identical `taskFieldsForRename`/`commitTaskTitle` door Quick Add's `addTask` already goes through, only ever setting a field a phrase actually resolved (`quick-add-task.test.ts`'s `taskFieldsForRename` suite, `task-title-commit.test.ts`, and `todo-page.test.tsx`/`composer-page.test.tsx`'s new rename cases). `rename-capture-2026-09-11.md` closes the gap the pre-#247 code cited as its own reason for staying unparsed: both Todoist rename surfaces resolve a phrase and strip it, matching the shape this row now builds — but that capture did not exercise a failed phrase or a phrase-free rename, so this app's own conservative "only ever set a field that actually resolved, never clear one the reader didn't touch" is still an unevidenced choice on our side, not a measured match. One further, unbuilt divergence that capture found: Todoist's own detail-dialog rename raises a toast naming the field it changed ("Date updated to Tomorrow", with Undo); meologue raises none. Still not `matched` — Todoist was driven for this capture, but nobody has driven meologue's own two rename surfaces side by side against it in the same session |
| DET-08 | While editing, the properties panel's Date control does **not** reflect an in-title recognition until save — unlike Quick Add's footer, which fills live | lifecycle.md | `task-detail-view.tsx` | `matched` | **Driven live on both sides (flow 4):** while ` tom` was recognised in the title and before Save, the Date control read "Date" in both; after Save it read "Tomorrow" in both. Artifacts: `live-audit-dom/flow4-DET-07-08-presave-*.json`, `flow4-DET-07-08-postsave-*.json`. The two surfaces stay unlinked exactly as observed: the Date attribute row still reads off `task.date`, never off the title editor's own live state, so there is structurally nothing for it to reflect early. Vacuous until #226 gives the title something to recognise, but the separation itself is real and deliberate. |
| DET-09 | Editing is **task-wide**: clicking the description puts title and description into edit together, sharing one Cancel/Save | lifecycle.md §1 | `task-detail-view.tsx`'s `editing`/`focusField`/`saveEditing` (issue #229) | `matched` | One shared form, one Cancel/Save pair, replacing the pre-#229 per-field `editingTitle`/`editingDescription` blur-commit shape. **Driven live on both sides (flow 4):** activation puts Task name and Description into edit together under a single Cancel and Save, in both. What Cancel does with unsaved edits differs — see DET-15. Artifacts: `live-audit-dom/flow4-DET-04-06-09-todoist.json`, `flow4-DET-04-06-09-meologue.json` |
| DET-10 | Clicking a generic part of the edit form focuses the **title**, not the description | lifecycle.md §1 | `task-detail-view.tsx`'s `focusField` | `divergent` | **Re-tested at a DOM-identified gap on both sides (flow 5): Todoist focuses the dialog, meologue focuses the title.** A real mouse click at the centre of the gap between the Task name and Description editors — Todoist at 610.5,154 (title bottom 150, description top 158), meologue at 627,153.7 (title bottom 147.7, description top 159.7) — put focus on Todoist's **dialog** and on meologue's **Task name** editor. So live Todoist does not do what `lifecycle.md` §1 records as verified (focus the title), and meologue does. meologue was not consistent across runs: flow 4's click at 615,153.7, taken without editor rects, landed on Description. Matching live Todoist means a generic click in the form should focus neither field. Low-severity fix candidate. Artifacts: `live-audit-dom/flow5-DET-10-todoist.json`, `flow5-DET-10-meologue.json`. **Earlier (flow 4), not settled:** At nearly the same screen position inside the edit form — Todoist 618,154, meologue 615,153.7 — a click focused the **dialog** in Todoist and the **Description** editor in meologue; a click lower in meologue's form (615,400) focused the dialog. Neither focused the title, which is what `lifecycle.md` §1 records as verified for "a generic part of the combined edit block". The two layouts differ, so matching coordinates are not the same region, and one point cannot overturn a verified claim about a region. **Re-test** by identifying the gap between the title and description editors in each app's own DOM and clicking that. Artifacts: `live-audit-dom/flow4-DET-10-todoist.json`, `flow4-DET-10-meologue.json`. Reproduced via the two entry points this view actually has: the description's own click target (the pill, or the rendered block) sets `focusField: "description"`; every other entry point (today, only the title's own button) sets `"title"` |
| DET-11 | The Description placeholder reads exactly "Description"; there is **no formatting toolbar** anywhere | lifecycle.md §1 | `task-description-editor.tsx`'s `TaskDescriptionEditor` default `placeholder` | `matched` | No toolbar element anywhere, matching the reference. **Driven live on both sides (flow 4):** the Description editor's placeholder reads "Description" in both — a `data-placeholder` attribute in Todoist, a non-editable ProseMirror widget in meologue — and neither shows any formatting toolbar. Artifacts: `live-audit-dom/flow4-DET-11-todoist.json`, `flow4-DET-12-todoist.json`, `flow4-DET-11-meologue.json`, `flow4-DET-12-meologue.json` |
| DET-12 | Description markdown renders **live as input rules** — bold, bullet list, inline code | lifecycle.md §1 | `task-description-editor.tsx` (`descriptionSchema`, `strongInputRule`/`codeInputRule`/`bulletListInputRule`) | `matched` | **Driven live on both sides with identical keystrokes (flow 4):** `**bold** ` became `<strong>bold</strong>`, `- item ` became a bullet list, and `` `code` `` became `<code>code</code>`, live, in both. One structural difference: Todoist's list item wraps its text in a paragraph (`<li><p>item</p></li>`), meologue's does not (`<li>item</li>`) — invisible, recorded rather than restatused. Artifacts: `live-audit-dom/flow4-DET-12-todoist.json`, `flow4-DET-12-meologue.json`. A bespoke, minimal ProseMirror schema — not `entrySchema` — for bundle-budget reasons (that file's own header comment). Covers exactly the three named constructs; a numbered list, heading or other markdown construct stays literal text rather than crashing |
| DET-13 | A bare URL in a description is **rewritten on save** to the fetched page title | lifecycle.md §1 | — | `divergent` | Network-dependent; deliberately not implemented (this ticket's own brief). `task-description-editor.tsx` recognises no URL at all |
| CMT-01 | Comments submit on **Ctrl/Cmd+Enter or the "Comment" button** — Enter and Shift+Enter both insert a newline | lifecycle.md §2 | `task-detail-view.tsx`'s `CommentComposer` `onKeyDown` | `matched` | Opposite of Quick Add, kept deliberately unmerged. **Driven live on both sides (flow 5):** Enter and Shift+Enter each insert a newline without submitting; Cmd+Enter submits; the Comment button submits — identical in both. Artifacts: `live-audit-dom/flow5-CMT-01-todoist.json`, `flow5-CMT-01-meologue.json` |
| CMT-02 | Comment markdown renders like the description's, but a bare URL keeps its text and gains an unfurl card | lifecycle.md §2 | `entryProse` (already shared with the description) | `divergent` | **Driven live with `**bold** and https://example.com` (flow 5), and the URL half differs twice over.** Both render `**bold**` as `<strong>`. Todoist wraps the URL in a real `<a href target="_blank">` and appends an unfurl card ("Example Domain"); meologue leaves the URL as plain text — **not a link at all** — with no card. So there are two gaps, linkification and the unfurl, where this row's earlier note assumed only the card was missing. Fix candidate. Artifacts: `live-audit-dom/flow5-CMT-02-todoist.json`, `flow5-CMT-02-meologue.json`. **Earlier note:** The unfurl card itself is not built (no network fetch anywhere in this view); the "keeps its text, no rewrite" half already matches since neither surface rewrites a URL |
| CMT-03 | Comment edit, delete and their confirmation wording | lifecycle.md §2 | `task-detail-view.tsx`'s `ConfirmDialog` (title "Delete comment?", body "This comment will be permanently deleted.") | `divergent` | **Driven live on both sides (flow 5): deletion matches, editing does not.** The delete confirmation is identical — "Delete comment?" / "This comment will be permanently deleted." / Cancel, Delete — differing only in role (`dialog` in Todoist, `alertdialog` in meologue). Editing is entered and committed differently: Todoist's comment menu (Edit · Copy text · Copy link to comment · Delete) opens an editor with explicit **Cancel** and **Update**; meologue's hover pencil swaps in a textarea that **saves on blur**, so an edit cannot be abandoned once focus leaves. Neither shows an "edited" marker. The run's summary called this row matched; the edit commit model is a real difference. Fix candidate. Artifacts: `live-audit-dom/flow5-CMT-03-todoist.json`, `flow5-CMT-03-meologue.json`. Ours used to delete with no confirmation at all |
| CMT-04 | Undo toast reads exactly **"1 task completed"** with an "Undo" button and a Close, in `role="alert" aria-live="polite"` | lifecycle.md §3 | `todo-page.tsx:390-395` — a completion toast already ships, with an Undo action | `divergent` | **Re-driven live on both sides (flow 5), and wording, role and lifetime all differ.** Todoist: "1 task completed" with Undo, on an element carrying `role="alert"` and `aria-live="polite"`, first seen at 360ms and gone between 10,775ms and 11,081ms. meologue: `Completed "ZZ probe complete"` with Undo, `aria-live="polite"` on an ancestor, and **no `role="alert"` anywhere on it** — the run's summary said an ancestor carried the role, but its artifact records `null` — gone between 4,346ms and 4,651ms. Todoist's ~11s is longer than the 6–8s `lifecycle.md` recorded. A Close button, which `lifecycle.md` lists, was not captured on either side. Artifacts: `live-audit-dom/flow5-CMT-04-todoist.json`, `flow5-CMT-04-meologue.json`. **Rot corrected** — this row read "nothing built" while `todo-page.tsx:390-395` already fires a toast with an Undo button on every completion. Not a match, though: wording is `Completed "<task name>"` (task-specific), where Todoist's is the task-agnostic, count-based `1 task completed`. Recorded as `divergent` rather than `built` because the two have already been compared and differ — but this is a discovered gap, not a deliberate decision, and still needs one |
| CMT-05 | The toast auto-dismisses after **6–8 seconds**, and Ctrl/Cmd+Z also undoes a completion | lifecycle.md §3 | same toast as CMT-04 (`todo-page.tsx:390-395`) | `divergent` | **Re-driven live on both sides (flow 5):** Cmd+Z undoes a completion in Todoist — the row's checkbox reads "Mark task as complete" again — and does **not** in meologue, verified against the store rather than the screen (the task stays in `listCompleted()` with `completedAt` set). Toast lifetimes as measured under CMT-04: ~11s in Todoist, ~4.5s in meologue. Artifacts: `live-audit-dom/flow5-CMT-05-todoist.json`, `flow5-CMT-05-meologue.json`. **Rot corrected** — this row read "nothing built" while the toast above already exists. It diverges on both halves of this row: lifetime is ~4s (sonner's own default; no `duration` override anywhere in the codebase), against Todoist's measured 6–8s; and `Ctrl/Cmd+Z` is genuinely **not bound** — no `mod+z` entry exists anywhere in `todo-keymap.ts:95-216`. As with CMT-04, `divergent` here marks "compared and found different," not a design choice |
| CMT-06 | Activity wording: `You added {task}`, `You completed {task}`, `You uncompleted {task}`, `You commented {content} on {task}`, `You added a description {content} to {task}`, `You changed the description of {task} to {content}`, `You removed the description {content} from {task}`, `You deleted a comment from {task}`, `You changed the name of {task}` | lifecycle.md §4 | `format-event.ts`'s `describeEventLine` (issue #229) | `divergent` | **Re-driven live on both sides (flow 5), and the wording differs beyond the quoting.** Todoist, in a task's own activity: "You added {task}", "You commented {content} on {task}" (content as an unquoted chip), "You deleted a comment from {task}", "You completed {task}", "You uncompleted {task}", ending "That's it. No more history to load."; **editing a comment records no event at all**. meologue, in a task's own Activity section: `You commented "{content}"` (quoted, no chip, no "on {task}"), "You deleted a comment" (no "from {task}"), "You completed" (no task name), plus an **"Edited a comment"** event Todoist does not have. meologue's missing "You added" is a seeding artefact: its fixtures were written straight to the store, which records no creation event. Fix candidate. Artifacts: `live-audit-dom/flow5-CMT-06-comments-task-todoist.json`, `flow5-CMT-06-complete-task-todoist.json`, `flow5-CMT-06-comments-task-meologue.json`, `flow5-CMT-06-complete-task-meologue.json`. **Overclaim corrected: only 5 of 9 templates are verbatim, not all nine.** The four carrying `{content}` (`format-event.ts:258` comment, `:377` removed-description, `:387` added-description, `:398` changed-description) wrap the content in literal quotation marks (`` `"${content}"` ``); Todoist's own DOM instead renders a clickable, unquoted content-preview chip in that position. The rename showing only the new name is correct, and Description changes were previously not recorded as an Event at all (`use-tasks.ts`'s `setDescriptionMutation`) until this ticket fixed it. Every other event type (moved, date/deadline/priority/labels, Project/Section) is untouched — not part of this row |
| CMT-07 | The activity list has **no filters** and ends with "That's it. No more history to load." | lifecycle.md §4 | `activity-feed.tsx`'s `completedOnly` narrowing, driven by a caller-owned toggle in `todo-page.tsx` | `divergent` | **Re-read live (flow 5), on two different surfaces that this row had merged.** *A task's own activity*: Todoist's "Task activity" dialog lists entries with no filter controls and ends with the literal line "That's it. No more history to load." — this row's claim, confirmed; meologue's per-task Activity section shows no terminal line. *The app-wide view*: meologue's `/todo/activity` has one filter, a "Completed only" checkbox, and no terminal line. Todoist's app-wide Reporting page (`/app/activity`) was **not established**: its first saved read is the error "Todoist couldn't load the required files.", 29 entries were read afterwards but the end of history was not reached, and the "rich filter controls" the run reported appear only as prose in its own note — no filter element was ever read. Artifacts: `live-audit-dom/flow5-CMT-06-complete-task-todoist.json`, `flow5-CMT-06-complete-task-meologue.json`, `flow5-CMT-07-todoist.json`, `flow5-CMT-07-meologue.json`. **We ship the opposite of this row, on purpose.** Issue #184 gave the feed a completions filter as a designed feature, so "no filters" is not a gap to close — it is a choice already made against it. Re-statused from `todoist-captured`, which wrongly implied somebody still owed the work. The terminal line ("That's it…") is genuinely absent and genuinely unbuilt; it is too small to carry a row of its own, so it is recorded here rather than promoted |
| CMT-08 | Italics, strikethrough, headings, numbered lists, blockquotes, fenced code blocks | live flow 5, one comment holding all six, entered identically: `*italic*` → `<em>`, `~~strike~~` → `<del>`, `# heading` → `<h1>`, `> quote` → `<blockquote>`, a fenced block → `<pre><code>`; **`1. first` stays a literal paragraph** (`live-audit-dom/flow5-CMT-08-todoist.json`) | live flow 5: `<em>`, `<s>`, and `1. first` → a real `<ol>`; **`# heading`, `> quote` and the fenced block all stay literal text** (`flow5-CMT-08-meologue.json`) | `divergent` | **Unblocked, and the gaps run in opposite directions.** meologue renders none of headings, blockquotes or fenced code in a comment; Todoist does not render a numbered list there. Fix candidate for meologue's three. Method note: typing the fence key by key corrupted Todoist's input — its live input rules read lone backticks as inline code — so both sides were entered as whole text. Previously `blocked`: not exercised |
| THEME-01 | Todoist's light-theme palette, type and density | pass2-2026-09-11.md §10 | `index.css`'s `[data-surface="todo"]` base block (issue #223) — meologue's own light design | `divergent` | **Closed as a deliberate divergence (user decision, 2026-09-11): meologue's light theme is its own design, not a replica, and light-mode parity rows are no longer tracked.** This is not a capture task left open — pass 2 eliminated every candidate cause: all 8 named themes plus an undocumented 9th render dark; the Sync theme and Auto Dark Mode toggles, a forced `prefers-color-scheme: light`, Chromium's own force-dark override (`Emulation.setAutoDarkModeOverride`), the Dark Reader extension (verified 0/0/0 on all three contamination signals) and the browser's launch flags were each ruled out; and macOS itself was switched to Light appearance and confirmed inert. No light palette is reachable on this account's Free plan tier, so no light-mode row could ever read `matched` — tracking them would promise a comparison that cannot be made |
| THEME-02 | Dark palette: content pane `rgb(31,31,31)`, sidebar/body ground `rgb(38,38,38)` — the secondary surface reads **lighter** than the page | scheduler-and-priority.md, measured directly | measured in-app: sidebar `rgb(38,38,38)`, content `rgb(31,31,31)` | `matched` | **Re-read live on both sides, same session (flow 6):** sidebar `rgb(38,38,38)`, content pane `rgb(31,31,31)`, identical in both — read off `<nav>`'s own background rather than an ancestor, after a first attempt walked past it to a shared parent and wrongly reported both panes the same colour. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`. Dark only. Initially failed: the sidebar painted nothing and inherited the content colour — caught by looking at a screenshot, not by any test |
| THEME-03 | Borders `rgb(61,61,61)`, muted text `rgb(204,204,204)`, focus ring `rgb(23,91,194)` | live flow 6: divider `rgb(61,61,61)`; muted text `rgb(204,204,204)` (read off the "My Projects" leaf after a first attempt picked a visually-hidden "Date:" label and returned white); focus ring `rgb(23,91,194) 0 0 0 1px inset` with a `rgba(255,255,255,0.14)` tint and no outline (`live-audit-dom/flow6-KBD-05-06-todoist.json`) | live flow 6: divider `rgb(61,61,61)`, muted text `rgb(204,204,204)`; **focus ring not yet measured** | `built` | **Two of the three colours now match live on both sides.** The focus ring is confirmed on Todoist's side only — meologue's is being read separately, and this row can close once it is. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`, `flow6-KBD-05-06-todoist.json` |
| THEME-04 | Type scale: row 14px/21px, chrome 13px, Quick Add title 16px/23px | live flow 6: row title 14px/21px, sidebar 13px (`live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`); composer 16px/23px from QA-20's own live run | live flow 6: row title 14px/21px, sidebar 13px (`…-meologue.json`); composer 16px/23px, QA-20 | `matched` | **All three sizes now read live on both sides**, the row and chrome sizes in flow 6 and the composer title under QA-20, which is already `matched`. Three sizes this app never had to distinguish |
| THEME-05 | The system font stack, not Geist, inside Todo | quick-add.md § fonts | measured in-app: resolves to `-apple-system, "system-ui", …` | `matched` | **Re-read live on both sides, same session (flow 6):** a task row title's computed `font-family` is byte-identical in both — `-apple-system, "system-ui", "Segoe UI", "Noto Sans", system-ui, sans-serif, "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji"`. This is the third independent confirmation, and the one QA-01's corrected note depends on. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`. Initially failed silently — `@theme inline` bakes `--font-sans`'s literal into the `font-sans` utility, so re-pointing the token was inert while every test stayed green. Fixed with a direct `font-family` on the scope |
| NAV-08 | The chrome/sidebar renders at 13px, smaller than the row text | row-and-detail.md, quick-add.md § fonts | measured in-app: 13px | `matched` | **Re-read live on both sides, same session (flow 6):** 13px at weight 400 in both. One difference recorded without failing the row, which is about size: the computed `line-height` is `normal` in Todoist against `19.5px` in meologue. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json` |
| NAV-09 | Content column: Todoist left-aligns an **800px** column beside a **280px** sidebar and puts the view name in it as a large heading (`<h1>`, 26px / weight 700 / 35px line-height, left-aligned flush with the column, **no separate app bar**); ours now caps the same column at **800px** above the 900px wide-layout breakpoint and renders the view name as an in-column `<h1>`, Back and the Sync dot relocated into that row, no separate app bar | pass2-2026-09-11.md §8 | `shell.tsx` (`columnWidthClassName` — the 800px cap, `w-[97%] md:w-[85%] min-[900px]:w-full min-[900px]:max-w-[800px]`; `hideAppBar` — the in-column heading row); `todo-page.tsx` (`todoHeading`, the view→heading mapping: Inbox/Today/Upcoming/Projects/Search/Activity/Filters & Labels/Labels, plus a Project's or Filter's own resolved name) | `divergent` | **Read live on both sides at 1470×836 (flow 6), and one half of this row's own premise is wrong.** The view-name heading matches exactly: a visible `<h1>` at **26px / 700 / 35px**, left-aligned, in both. meologue's content column measured **800px** (left 499 → right 1299), the cap engaged above the 900px breakpoint. **But "no separate app bar" is false for Todoist**: it has **two `<header>` elements**, one labelled "Header: contains title, subtitle, actions and options related to the current view", and meologue has **zero**. The corpus read the view name as an in-column heading and concluded there was no app bar; the landmark is there regardless. Two honest limits: Todoist's own column width was not captured this run (the reading taken was its 1190px main region, not the column — the 800px figure still rests on pass 2), and the first `<h1>` found in Todoist was an invisible no-JS fallback reading "Todoist couldn't load the required files.", re-queried for visibility. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`. **Built against issue #254, not yet `matched`.** jsdom lays out nothing, so `shell.test.tsx`/`todo-page.test.tsx` pin the classes and the mapping, not the pixel values — 800px and 26px/700/35px are still unverified on a real screen; nobody has driven the two apps side by side. **One divergence, deliberate and recorded in ADR 0019's own amendment:** meologue's cap engages at **900px**, not Todoist's own sidebar-driven breakpoint — reusing the existing wide-layout breakpoint (where Todo's sidebar and bottom bar already change) rather than adding a second transition. **A consequence, also recorded there:** Back and the Sync dot now scroll away with the column instead of staying pinned in a fixed bar — matching Todoist's own behaviour, but a real loss on a long Inbox. Previously: **Estimate replaced with measured values.** This row previously guessed "~750px" with no measurement; pass 2 measured the content column at 800px (left 468 → right 1268 at a 1470px viewport), the sidebar at 280px, and the view-name heading at 26px/700/35px — and confirmed there is no separate app bar at all, the view name being the in-column heading itself. **Citation corrected (carried forward):** this row previously cited `pairs/NAV-01-meologue.png`, a file that does not exist, in a directory that does not exist, under another row's ID — see this file's own note on `pairs/` above |
| NAV-10 | Todoist's add-task affordance is a subtle "+ Add task" row at the **end of the list**, with Quick Add as an overlay; ours is a bordered input with an "Add" button at the **top** | quick-add.md § Quick Add chrome | screenshot | `built` | **Read live on both sides (flow 6), but meologue's reading is not trustworthy and no verdict is recorded.** Todoist: a `<button>` reading "Add task", **14px**, `rgb(128,128,128)` — matching pass 2 — sitting after the last *active* row and before the completed rows that interleave below it (ROW-14), which is why its DOM position reads "before-last-row" against the list's true last `<li>`. meologue: the element captured was a `DIV` reading "Add a Task" at **16px, `rgb(255,255,255)`**, after the last task row. That contradicts #252's own measured 14px/`rgb(128,128,128)` resting placeholder, and the captured tag suggests the **editor box was measured rather than the placeholder leaf** — the same wrong-element class of error QA-16 and NAV-09 hit in this audit. **Re-read the placeholder leaf specifically** (the ProseMirror placeholder widget, not its container) before treating the size or colour as a divergence. The text differs too — "Add task" against "Add a Task" — which is not element-dependent. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`. **Position and weight fixed by issue #252.** `add-task-form.tsx`'s render moved from before Inbox/Today/a Project's own list to just after it (`todo-page.tsx`), and the field's resting border was dropped (`EDITOR_BOX_CLASSES`'s `border-input` → `border-transparent`). Not `matched`: the 14px/`rgb(128,128,128)` figures are a real-browser measurement nobody has taken against ours yet (jsdom computes no layout), and see NAV-12 for the part of this row #252 deliberately left unbuilt — the click-to-reveal composer, still an overlay-free always-mounted field here |
| NAV-01 | Sidebar order: Add task · Search · Inbox / Today / Upcoming / Filters & Labels with counts · **Reporting** · "My Projects" tree | row-and-detail.md, live sidebar; pass2-2026-09-11.md §9 | `todo-sidebar.tsx` | `divergent` | **Read live on both sides, same session (flow 6).** Todoist, in DOM order: Add task (a `<button>`) · Dictate tasks with Ramble · Search (button) · Inbox · Today · Upcoming · Filters & Labels · **Reporting** · **My Projects** (an `<a>` to `/app/projects`). meologue: Add task (an `<a>` to `/todo/inbox`) · Search · Inbox · Today · Upcoming · Filters & Labels · **Activity** · **Projects** (an `<h2>`, not a link). Three differences beyond Ramble, which is a Todoist product feature: the activity destination is labelled **Reporting** against meologue's **Activity**; the projects heading is **"My Projects"** and a link, against **"Projects"** and a heading; and the counts live in different places — Todoist puts them in `aria-label` ("Inbox, 9 tasks") with nothing in the text, meologue puts a digit in the text ("Inbox4") with no `aria-label`. Todoist's Today entry also carries the date ("12Today"), which meologue's does not. Fix candidates. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`. **Corrected against pass 2's live sidebar.** This row's order previously omitted a **Reporting** row (→ `/app/activity`) that the live sidebar carries, and read the projects heading as "Projects" where it actually reads **"My Projects."** Live order in full: Add task · Search · Inbox · Today · Upcoming · Filters & Labels · Reporting · My Projects, plus a "Dictate tasks with Ramble" button beside Add task. `todo-sidebar.tsx` has neither Reporting nor the "My Projects" wording — see the new `NAV-11` row for the Reporting/Activity gap specifically |
| NAV-02 | The sidebar renders only inside `/todo/*`, at ≥900px, in the pane the shell already has | ADR 0076 | `chat-shell-layout.tsx` | `built` | No second pane, divider or width mechanism. **Citation corrected (issue #248):** this row cited "ADR 0070," which two different ADRs shared — the number was disambiguated, and the Todo-navigation ADR is now 0076 |
| NAV-03 | Below 900px there is no sidebar and the bottom bar remains | ADR 0076 | `chat-shell-layout.test.tsx` | `built` | Held by a test, not an assertion — this is what keeps Android intact. **It was not enough.** This row holds that the bottom bar *remains*; nothing held that every destination is reachable *from* it, and Upcoming (#223) was added to `todo-sidebar.tsx` alone — leaving it unreachable on any touch device for the life of this branch, since the only other path was a keyboard shortcut. Todo has two navigations, not one, and a new destination has to enter both. **Citation corrected (issue #248):** see NAV-02's own note on the ADR renumbering |
| NAV-04 | Scoped `<nav aria-label="Todo">` with real hrefs and `aria-current` | live flow 6: Todoist's sidebar is a `<nav>` with **no `aria-label`**, and **no element anywhere carries `aria-current`** (`live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`) | live flow 6: `<nav aria-label="Todo">`, entries are real `<a href>`, and the current entry carries `aria-current="page"` (`…-meologue.json`) | `divergent` | **Compared live, and meologue is the more accessible of the two.** This row was written from meologue's own `chat-list.tsx` precedent, not from Todoist, and nobody had checked what Todoist does: it names neither the landmark nor the current page, so assistive technology gets no "you are here" and no way to distinguish this nav from any other. Matching Todoist exactly would mean deleting both. **A decision for the user, not a fix candidate** — the same shape as DET-03 and ROW-12 |
| NAV-05 | Upcoming day headings read `10 Sep ‧ Today ‧ Thursday`; only today and tomorrow get a relative word | scheduler-and-priority.md §9 | `task-views.ts` `upcomingDayHeading()` | `matched` | **Driven live on both sides (flow 2)** — see DATE-05: the two headings read in Todoist match meologue's byte for byte. Artifacts: `live-audit-dom/flow2-DATE-05-NAV-05-todoist.json`, `flow2-DATE-04-DATE-05-NAV-05-meologue.json` |
| NAV-06 | "Filters & Labels" is one destination covering both, with a combined count | live sidebar | `todo-sidebar.tsx` links to `/todo/filters`, which now renders both a Filters list and a Labels section (`filters-view.tsx`); the sidebar's own count is `filters.length + labels.length` | `built` | **Read live on both sides (flow 6):** one destination in both. Todoist's page carries `<h1>` "Filters & Labels" plus `<h2>` **"My Filters"** and `<h2>` "Labels"; meologue's carries the same `<h1>` and `<h2>` "Labels" but **no "My Filters" heading**. The combined-count half could not be tested — neither sidebar entry rendered a count, meologue's because the account holds 0 filters and 0 labels, Todoist's because its entry shows no count at all in this state. Stays `built` for that reason. Artifacts: `live-audit-dom/flow6-NAV-06-todoist.json`, `flow6-NAV-06-meologue.json`. #229's second half. Todoist's own screen puts full inline editing for both nouns on the one page; here, Labels' own full create/rename/recolour/delete lives one hop away at `/todo/labels` (`labels-view.tsx`), reached via this page's "Manage Labels" link — the destination and the count now genuinely cover both, but the *editing* surface is one link deeper for Labels than for Filters. Not driven side by side against the real app |
| NAV-07 | "Add task" in the sidebar opens Quick Add from anywhere | live sidebar | links to `/todo/inbox` | `divergent` | No global Quick Add exists yet; #226 owns it |
| NAV-11 | Todo's Activity view has no path to it from the sidebar at ≥900px | live app | `todo-sidebar.tsx` now links `/todo/activity` (a `CountRow`, no count badge — Activity is a log, not a pending count) | `divergent` | **Read live on both sides (flow 6): the destination is reachable from both sidebars, and the label differs.** Todoist's entry reads **"Reporting"** (`aria-label="Reporting"`, href `/app/activity`); meologue's reads **"Activity"** (no `aria-label`, href `/todo/activity`). The reachability this row exists for is satisfied on both; the wording is a fix candidate, tracked with NAV-01's other label differences. Artifacts: `live-audit-dom/flow6-NAV-01-04-06-08-09-10-11-THEME-02-03-04-05-todoist.json`, `…-meologue.json`. **Fixed by issue #248.** Previously `/todo/activity` was linked only from `todo-nav.tsx:78` (which hides itself at ≥900px, per NAV-03) and from inside a Project (`project-view.tsx:217-218`); `todo-sidebar.tsx` — the nav that replaces `todo-nav` at that width — had no Activity entry anywhere. The identical defect class NAV-03's own note already names for Upcoming: a destination reachable from one navigation but not the other is unreachable on desktop, since the only other path is a keyboard shortcut Activity has none of. Not `matched` — nobody has driven the two sidebars side by side for this row |
| NAV-12 | Todoist's real add-task affordance is a **static label** that expands into Quick Add on click, with its own date/priority/project pickers, focus handling and escape-to-collapse | quick-add.md § Quick Add chrome | — | `divergent` | **Named, deliberately deferred — issue #252's own scope note.** meologue's field is always-mounted (never unmounts to a bare label) and its Add button is rendered-but-disabled rather than absent, so `add-task-form.test.tsx` and `apps/e2e/tests/todo.spec.ts` keep working against a field that exists before any click. Folding the click-to-reveal composer in here would break both for no parity gain that NAV-10's position/weight fix doesn't already deliver on its own. Owned by a future ticket, not by #252 |
| THEME-06 | The token scope must reach **portaled** overlays — Radix mounts every dialog, menu, sheet and popover into `document.body` | n/a (our own architecture) | `data-surface` written to `documentElement` by `chat-shell-layout.tsx` | `matched` | Found by measuring: the detail dialog rendered `insideScope: false`, painted `oklch(0.205 0 0)` and set in Geist, unthemed since #223. A subtree scope cannot cover a portal; `documentElement` is above every portal by construction |
| THEME-07 | The dark override must use the compound selector `.dark[data-surface="todo"]`, not only the descendant form | n/a (our own architecture) | `index.css` carries both | `matched` | Moving the attribute to `documentElement` put `.dark` and `data-surface` on the **same** element, so the descendant selector stopped matching and Todo silently fell back to the light palette. Caught by reading white where the page was `rgb(31,31,31)` |
| DET-14 | The detail modal is background `rgb(31,31,31)` — the same ground as the content pane, not a raised surface — radius **10px**, no border, shadow `rgba(0,0,0,.16) 0 2px 8px`, 864x772 | measured live in Todoist | `--card`/`--popover` now follow `--background`; `task-detail-view.tsx`'s wide dialog now carries `rounded-[10px]` | `divergent` | **Settled (flow 5): width 864 in both; height 708 in Todoist against 710.594 in meologue, in the same 1470×836 viewport.** meologue's opening animation was stuck at `currentTime` 0 in the automated tab, so it was driven to its resting keyframe with `Animation.finish()`; afterwards `transform` was `none` and two reads agreed at 864×710.594 — exactly the flow-4 arithmetic below. Todoist had no running animation and read 864×708 twice. Radius, background, border and shadow already matched. The 2.6px height gap comes from different height formulas and will vary at other viewport heights. Low-severity fix candidate. Artifacts: `live-audit-dom/flow5-DET-14-todoist.json`, `flow5-DET-14-meologue.json`. **Earlier (flow 4):** Driven live on both sides at the same 1470×836 viewport (flow 4): radius `10px`, background `rgb(31,31,31)`, no border and shadow `rgba(0,0,0,0.16) 0 2px 8px` identical; width matches; height differs by ~2.6px.** The raw readings looked like a width defect — Todoist 864×708, meologue 820.8×675.06 — but meologue's reading was taken mid-way through its `data-open:zoom-in-95` opening animation, and `getBoundingClientRect` includes transforms. Every meologue number is exactly 0.95 of its resting value: 864×0.95 = 820.8; its `h-[min(48.25rem,85vh)]` at an 836px viewport is 710.6, ×0.95 = 675.07; and centring those in 1470×836 gives the recorded left 324.6 and top 80.47. Todoist's own rect confirms the same viewport ((1470−864)/2 = 303; 64+708+64 = 836). So resting width is **864 in both**, and resting height is **708 in Todoist against 710.6 in meologue** — different formulas (Todoist's looks like `100vh − 128px`; meologue's is `min(772px, 85vh)`), which also means the corpus's 864×772 was read at a taller viewport. Stays `built` because meologue's resting size is derived, not read: **re-measure after the animation settles.** A text-size setting was ruled out as the cause — `data-text-size` only sets `--entry-text-scale` for Entry prose (`index.css:303-314`) and never touches the root font size. Artifacts: `live-audit-dom/flow4-DET-02-03-05-14-todoist.json`, `flow4-DET-02-03-05-14-meologue.json`. Background and font already matched. Radius fixed in #229: `rounded-xl` (14px) → `rounded-[10px]`, measured in-app via `getComputedStyle` after the change |
| DET-15 | Cancelling an edit that holds unsaved changes asks first: "Discard unsaved changes?" / "Your unsaved changes will be discarded." with Cancel and Discard | lifecycle.md §1 ("Cancel with unsaved changes", verified); re-driven live (flow 4) after typing into the Description: the dialog appeared with exactly that text (`live-audit-dom/flow4-DET-12-todoist.json`) | live flow 4: Cancel discarded the same unsaved Description edits **immediately, with no confirmation** (`flow4-DET-12-meologue.json`) | `divergent` | **New row.** The nuance was already verified in `lifecycle.md` but had no ledger row, so nothing ever compared it. meologue has no guard against losing unsaved edits in the detail view. Fix candidate |
| DET-16 | Saving a rename whose phrase resolves a date raises a toast naming the change — "Date updated to Tomorrow" — with an **Undo** action, for about 10 seconds | `rename-capture-2026-09-11.md`; re-driven live (flow 4): polled every ~500ms after Save, the toast "Date updated to Tomorrow" with Undo was present through 9,609ms and gone by 10,119ms (`live-audit-dom/flow4-DET-07-08-postsave-todoist.json`) | live flow 4: no toast of any kind across 24 polls over 11.6s after the same save (`flow4-DET-07-08-postsave-meologue.json`); meologue records the change only as an Activity entry ("Set the date to Sep 13") | `divergent` | **New row**, split out of DET-07's note, which had recorded the missing toast as an unbuilt divergence. Fix candidate. Its ~10s lifetime is longer than the ~6–8s `lifecycle.md` measured for the completion toast (CMT-05), so the two toasts should not be assumed to share a duration |
| STR-01 | Delete a Project: dialog reads "Delete project? The [name] project and all its tasks will be permanently deleted. This action cannot be undone.", buttons Cancel/Delete | quick-add.md § "Destructive confirmation wording" | `project-view.tsx`'s new `ConfirmDialog`, verbatim; `use-projects.ts`'s new `removeProject` reaches `ProjectStore.removeProject` (existed since #171, wired to no UI until now) | `built` | Driven in a real browser: created a Project, opened it, deleted it, read the dialog text back |
| STR-02 | A Project's colour is changeable after creation, not fixed at creation time | 171-brief.md's own acceptance criteria (create/rename/recolour/favourite/archive/delete) | `project-view.tsx`'s header colour `<select>`, reaching `ProjectStore.setProjectColour` (existed since #171, wired to no UI until now) via `use-projects.ts`'s `setProjectColour` | `built` | Driven in a real browser: opened a Project, changed its colour, saw the dot update |
| STR-03 | Delete a Label: dialog reads "Delete label? The [name] label will be permanently deleted.", buttons Cancel/Delete | quick-add.md § "Destructive confirmation wording" | `labels-view.tsx`'s new `ConfirmDialog`, verbatim | `built` | Driven in a real browser: created a Label, deleted it, read the dialog text back |
| STR-04 | A Label can be created, renamed and recoloured from a management screen, not only as a side effect of `@name` in quick-add | CONTEXT.md's Label entry; `use-labels.ts`'s own pre-#229 header comment naming this exact gap | New `/todo/labels` route, `labels-view.tsx` (create form, inline rename-on-blur, per-row colour `<select>`); `use-labels.ts`'s new `addLabel`/`renameLabel`/`setLabelColour`, reaching `LabelStore.rename`/`setColour` (existed since #170, wired to no UI until now) | `built` | Driven in a real browser: created "Test", renamed it, recoloured it |
| STR-05 | Todoist's Label options menu (Edit · Add to favorites · Move to shared labels · Copy link to label · Delete), reproduced minus what doesn't apply | quick-add.md § "Menus seen in passing" | `labels-view.tsx`'s own header comment: only Edit (inline rename field) and Delete survive — no Label favourite or shared Labels exist in this app (`label-types.ts` carries neither field), and no per-Label route exists for a "copy link" to point at | `built` | Not a literal menu widget — inline controls doing the same two jobs. Not driven against the real app (this app has no Label favourite/sharing to compare) |
| STR-06 | Create/rename/recolour/delete for Filters | 185-brief.md's own acceptance criteria | `filter-view.tsx`/`filters-view.tsx` already carried all four before this ticket (`addFilter`/`renameFilter`/`setFilterColour`/`removeFilter`, `use-filters.ts`) | `built` | Verified reachable, not newly built — this ticket's own gap analysis found Filters already complete; Filter carries no `favourite`/`archived` field (`filter-types.ts`), so those two are not applicable, matching Project's own type being the one of the three with both flags |
| STR-07 | Sections' existing create/rename/reorder/archive/delete (built in #171) is reachable and consistent with this ticket's own new Project/Label controls | 171-brief.md's own acceptance criteria | `project-view.tsx` — unchanged by this ticket, confirmed reachable from `/todo/projects/:id` and left as-is (the destructive count-then-confirm shape it already had is what this ticket's own new Project-delete confirmation was modelled on) | `built` | Read, not rebuilt. Section's own delete dialog wording ("This destroys N Tasks…") predates the verbatim Todoist wording table this ticket works from and was left untouched — not a row that table names |
