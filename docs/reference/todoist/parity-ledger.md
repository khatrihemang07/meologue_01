# Todoist parity ledger

One row per nuance. This file is the checklist the Todo rebuild is measured against, and it is the
definition of done — a ticket lands when every row it owns reads `matched` or `divergent`.

Rows are generated from the reference docs in this directory as those are written, never recalled
from memory afterwards. If a behaviour has no row here, it was never established, and code that
claims to replicate it is claiming something nobody checked.

## Status vocabulary

| Status | Meaning |
|---|---|
| `todoist-captured` | We know what Todoist does. Nothing has been built. |
| `built` | meologue implements it. **Not a terminal state** — nobody has compared them. |
| `matched` | Driven in both apps side by side and compared on all three axes below. |
| `divergent` | Deliberately different. The Note column says why, and whose decision it was. |
| `blocked` | Cannot be established. The Note says what stopped it. |

`built` is called out because conflating it with `matched` is precisely how the implementation this
rebuild replaces came to diverge so widely while looking finished.

## What `matched` requires

Pixel-diffing two different applications cannot work — different content, different account,
different strings. So a row is `matched` only when all three agree:

1. **Behavioural** — same input, same state transition. The axis that decides correctness.
2. **Structural** — the DOM contract: same elements, attributes, ARIA roles and labels.
3. **Dimensional** — measured `getComputedStyle` values with both apps showing deliberately
   identical content: row height, font family/size/weight/line-height, colour as rendered `rgb`,
   padding, border-radius, checkbox diameter, indent step.

Screenshots are captured for human review and belong in `pairs/`, named `<ID>-todoist.png` and
`<ID>-meologue.png`. They are evidence for a reader, not the pass criterion — two screenshots can
look alike while the behaviour differs.

**`pairs/` does not exist yet, and no row has ever cited a file in it.** Every row reading
`matched` cites measured values instead — which is the pass criterion, and is sufficient. The
convention stands for whichever capture pass first fills it; until then, read a row's own evidence
column rather than looking for a picture nobody took. Said here because a promised-but-absent
convention is the same defect as a stale row: it invites a reader to trust something that is not
there.

## What a status is pinned to

> **Superseded by [ADR 0077](../../adr/0077-parity-is-proved-live-not-against-a-dated-capture.md).**
> Rows are now established by driving **both** applications in the same session, against Todoist's
> live behaviour, with the raw artifacts committed beside the prose. The rule below is kept because
> its reasoning was weighed and overridden rather than found wrong — read it as the argument on the
> other side, not as current practice. The short version of why it lost: pinning defended against
> drift, which never actually bit, while the real failure was pinned prose whose evidence nobody
> could re-open. Two false claims propagated out of that gap — see the ADR.

**Rows are pinned to a dated capture, never to the live product.** Todoist ships changes; this
ledger is a definition-of-done for a rebuild, and a definition-of-done cannot chase a moving
target — "done" would never be reachable.

So a row's Todoist-evidence column names the capture it was verified against
(`quick-add.md`, `pass2-2026-09-11.md`, …). A row becomes stale only when a **later capture pass
supersedes it**, at which point that pass restatuses it explicitly. A row never silently goes wrong
because Todoist shipped something.

**The worked example this rule exists for.** `SCHED-02` recorded the scheduler's quick options as
`Today · Tomorrow · This weekend · Next week`, verified against the 10 Sep capture. On 11 Sep the
live product read `Today · Tomorrow · Next week · Next weekend` — "This weekend" was gone. Nothing
in this repo broke; the reference moved. That row therefore keeps its status against the capture it
was written from, with a note pointing at `pass2-2026-09-11.md` §5.

**The cost, stated plainly:** this ledger is knowingly behind whatever Todoist shipped most
recently. That is the deliberate trade. Drift is reconciled by running a full re-capture at the
start of each parity programme and restatusing whatever moved — not by chasing changes between
programmes.

## ID scheme

| Prefix | Area |
|---|---|
| `QA-` | Quick add: recognition, withdrawal, the token vocabulary, Quick Add chrome |
| `ROW-` | Task row: anatomy, checkbox, hover, metadata line, long titles |
| `DATE-` | Date wording, tone and colour, wherever a Date renders |
| `SCHED-` | The scheduler: quick options, calendar, time, deadline, recurrence |
| `PRI-` | Priority: pickers, display, ordering |
| `DET-` | Task detail view: layout, title editing, description, sub-tasks |
| `CMT-` | Comments and the activity log |
| `KBD-` | Keyboard shortcuts, focus movement, focus appearance |
| `NAV-` | Sidebar, destinations, counts |
| `STR-` | Projects, Sections, Labels, Filters |
| `THEME-` | The design-token scope itself: palette, type, density, radii |

## Ledger

_Rows are appended as each reference doc in this directory is written._

| ID | Nuance | Todoist evidence | meologue evidence | Status | Note |
|---|---|---|---|---|---|
| QA-01 | A recognised match is an `inline-block` span with 4px horizontal padding — it occupies width, so following glyphs shift | quick-add.md § the one fact; pass2-2026-09-11.md §4 (`tod` recognised: 32.31px wide, 16px/23px) | driven in-app: `tod` recognised renders `inline-block`, padding 4px/4px, 29.41px against 21.41px withdrawn | `built` | **Downgraded from `matched` — overclaimed** (pre-#251). Matched on *behaviour* (inline-block, padding appears/disappears, an 8px delta on withdrawal both sides show) but not on the quoted pixel values: ours measured 29.41px/21.41px against Todoist's own 32.31px/24.31px. Root cause: `EDITOR_BOX_CLASSES` (`add-task-form.tsx`) ended `md:text-sm md:leading-[22px]` → 14px/22px at desktop width, against Todoist's measured 16px/23px, while `index.css` already declared `--td-composer-title-font-size: 16px`/`-line-height: 23px` with zero consumers. **Fixed by #251:** the component now reads those two tokens at all widths instead of hardcoding a size beside them. **Re-measured in a real browser (`verification-2026-09-11.md`), and the pixel target is NOT met — in the other direction.** With the font fix in place and confirmed at 16px/23px, the chip measures **36.14px recognised / 28.14px withdrawn** against Todoist's 32.31/24.31: a constant **+3.83px in both states**, while the recognised−withdrawn delta is **8.00px in both applications**, exactly the 4px+4px padding this row is actually about. So the padding is right and is not the variable; the residue is **glyph width** — the same string at the same size in a different typeface (Geist here, Todoist's own stack there). **#251's stated reasoning was wrong**: it held that the chip was narrow *because* the font was small, so raising 14px→16px would land on 32.31px; the font rose and the chip overshot. **This pixel target is unreachable while the two applications render different fonts**, and no padding or font-size work closes it — the honest choices are to match the font stack (a far larger decision, with no corpus evidence behind it) or to restate this row's target as the 8px delta, which *is* matched and *is* implementation. Stays `built`, not `matched` |
| QA-02 | The recognised span carries `data-testid="natural-language-match"` and `data-match-id` holding the **resolved** value | quick-add.md § the span | driven in-app: `span[data-testid=natural-language-match][data-match-id]` | `matched` | One divergence: our `data-match-id` carries the raw resolved value (`2026-09-10`, `P1` for typed `p1`) where Todoist's carries a display form (`10 Sep`). Not user-visible; recorded rather than fixed. (`P4` was this row's example before #251 corrected `matchIdForToken`'s stored→UI priority inversion — see QA-12) |
| QA-03 | Recognised state is marked by `data-highlighted-match="true"` | quick-add.md § the span | driven in-app: `data-highlighted-match="true"` while recognised, attribute absent once withdrawn | `matched` | Driven side by side |
| QA-04 | First Backspace after a match withdraws recognition and **deletes no character** | quick-add.md § recognition and withdrawal | driven in-app: first Backspace leaves the text `tod` unchanged and drops only the highlight | `matched` | The behaviour this whole programme started from, now reproduced |
| QA-05 | Second Backspace deletes a character and removes the span | quick-add.md § recognition and withdrawal | driven in-app: second Backspace yields `to` and removes the span | `matched` | Driven side by side |
| QA-06 | Withdrawn span persists as the **same element**, restyled to `inline` with no padding | quick-add.md § the span | driven in-app: the same element persists through withdrawal, restyled from inline-block to inline with padding dropped | `matched` | One span, two states, removed only when the text changes |
| QA-07 | Withdrawal is per-occurrence and one edit deep — editing back to the same text re-recognises | quick-add.md § withdrawal is not sticky | `todo-quick-add-recognition.ts` `remapWithdrawnSpans` — positional, not signature-keyed (deliberately the opposite of `quick-add-highlight.ts`'s own model) | `built` | Covered directly by `todo-quick-add-recognition.test.ts`, including the `retype-03`/`retype-04` DOM captures' own round trip. Not verified in a real browser end to end. |
| QA-08 | Moving the caret into a recognised span does not withdraw it | quick-add.md § other ways to reject | `todo-quick-add-recognition.ts` `quickAddRecognitionPlugin` only reacts to `Backspace` in `handleKeyDown`; a caret move alone never touches its state | `built` | Not verified in a real browser (see QA-01's note). |
| QA-09 | Date vocabulary: today/tod/tomorrow/tmr/weekday/next week/in N days/25 dec/12/25 | quick-add.md § vocabulary | `en.ts:61-70` (relative-day words); `date-rules.ts:165-186` (weekday), `:206-226` (in N days), `:241-260` (next week), `:94-136` (25 dec / 12/25) | `built` | **Status was stale** — every form in this row's own vocabulary list has been parsed since #226; the row still read `todoist-captured` with a blank evidence column. Not `matched`: nobody has driven the two Quick Adds side by side |
| QA-10 | Time vocabulary: `at 5pm`, `5pm`, `17:00` | quick-add.md § vocabulary | `quick-add/date-rules.ts` `matchExplicitTime` — the optional `at`, the 12-hour-plus-meridiem branch and the 24-hour `HH:MM` branch; reaches a Task through `taskFieldsFromQuickAdd` (`lib/quick-add-task.ts`) and is decorated as a `time` match (`lib/todo-quick-add-recognition.ts`) | `built` | **Status was stale** — all three forms have been parsed and highlighted since #226, and this row still read `todoist-captured`. Pinned by `quick-add.test.ts`. Not `matched`: nobody has driven the two Quick Adds side by side |
| QA-11 | Recurrence vocabulary, with `daily` normalised to "every day" | quick-add.md § vocabulary | `date-rules.ts:375-388` (`matchRecurrenceWord`); `en.ts:116-124` (`recurrenceWords` table); `quick-add-task.ts:123-131` (`RECURRENCE_WORD_TO_PHRASE`, `daily` → `"every day"`) | `built` | **Status was stale** — implemented and evidence column left blank. Not `matched`: nobody has driven the two Quick Adds side by side |
| QA-12 | `p1`–`p4` recognised as priority, showing no date control | quick-add.md § vocabulary | `quick-add/rules.ts` `matchPriority` (`/\bp([1-4])\b/gi` through `storedPriorityOf`), decorated as a `priority` match by `todo-quick-add-recognition.ts` `matchIdForToken`, which crosses the stored→UI inversion back via `uiPriorityOf` so typed `p1` carries `data-match-id="P1"` and typed `p4` carries `"P4"` | `built` | **Status was stale.** The second clause ("showing no date control") is true only vacuously: Quick Add has no footer controls of any kind to show or hide — see QA-15, which is the row that actually owns that gap. **#251:** `matchIdForToken` previously read the stored priority number directly, so typed `p1` (stored 4) emitted `data-match-id="P4"` — the inverted identifier, invisible only because nothing rendered a chip from it yet. Now crossed via `uiPriorityOf`, confirmed independently by `docs/reference/todoist/rename-capture-2026-09-11.md` (`priority_4` is Todoist's own storage class for UI "P1") |
| QA-13 | `#` project popup, filtering, with "Project not found. Create X" fallback | quick-add.md § autocomplete | — | `todoist-captured` | |
| QA-14 | `@` label popup with "Label not found. Create X" fallback | quick-add.md § autocomplete | `rules.ts` `matchLabel` now matches `@label`, not `%label` (`%` dropped, not kept as an alias) | `built` | #226's first half (this row's parser side only): `packages/core/src/quick-add/` recognises `@` as the label sigil. The autocomplete popup itself is untouched — #226's own follow-up half. **Stale caveat dropped:** this note used to claim "apps/web's own `%`-sigil assumptions … are untouched"; `promote-tasks.ts:93-104` already routes labels through the identical `@`-based parser (`taskFieldsFromQuickAdd`), and no `%` sigil exists anywhere in `quick-add-task.ts` or `promote-tasks.ts` |
| QA-15 | Footer controls mirror the parse and gain Remove buttons once set | quick-add.md § Quick Add chrome | — | `todoist-captured` | |
| QA-16 | Quick Add is 580×97px at rest, radius 12px, padding 16px | quick-add.md § Quick Add chrome | — | `todoist-captured` | Dark theme values. **Trap for whoever builds this:** `--td-composer-background`/`-border`/`-radius` are already declared in `index.css` and have **zero consumers anywhere in the repo** — the real Quick Add is a full-width bordered row holding a 32px field (`add-task-form.tsx`). A declared token is not an implementation, which is the same class of defect as `@theme inline` baking `--font-sans` (THEME-05's own note) |
| QA-17 | The title field is the only contenteditable — no description input by default | quick-add.md § Quick Add chrome | `add-task-form.tsx` renders the shared `TaskTitleEditor` with `quickAddRecognitionPlugin` | `built` | **This row was stale and said the opposite.** It claimed the field was still a plain `<input>`, which was true when #225 deliberately deferred the conversion and false the moment #226 completed it. Caught by the branch's own spec review, not by any test — a ledger row asserting something untrue is worse than a missing one, since the whole file exists to be trusted |
| QA-18 | `Tab` from the title moves to More actions | quick-add.md § Quick Add chrome | — | `todoist-captured` | |
| QA-19 | `Shift+Enter` **submits** rather than inserting a newline | quick-add.md § Shift+Enter | `task-title-editor.tsx`'s keymap binds `Shift-Enter` to `options.commit()`, with a comment citing this row by name; `add-task-form.tsx` wires `onCommit` to the submit path | `built` | **Status was stale** — #225 built this and cited QA-19 while doing so, and the row was never updated. User had independently flagged this |
| QA-20 | Quick Add title renders at 16px/23px against 13px chrome | quick-add.md § fonts | Both sides now built: the 13px chrome is `--td-chrome-font-size`, applied in `todo-sidebar.tsx`; the Quick Add title is `--td-composer-title-font-size`/`-line-height` (16px/23px), now read by `add-task-form.tsx`'s `EDITOR_BOX_CLASSES` (`text-[length:var(--td-composer-title-font-size)] leading-[length:var(--td-composer-title-line-height)]`) at every width instead of a hardcoded `md:text-sm md:leading-[22px]` beside the unused token | `built` | **#251 fixed the dead-token trap this row recorded** — the title tokens had zero consumers before. Not `matched`: nobody has driven the two Quick Adds side by side with a real-browser pixel measurement to confirm the computed sizes agree, only that the component now consumes the declared tokens. See QA-16 for the still-open composer-chrome instance of the same trap (different tokens: background/border/radius, untouched by this fix) |
| QA-21 | Arrow-key navigation inside autocomplete popups | quick-add.md § autocomplete | — | `blocked` | Capture inconclusive; not evidence of absence |
| QA-22 | Deadline syntax in Quick Add | — | — | `blocked` | Never reached |
| QA-23 | Plain `Enter` isolated from `Shift+Enter` | — | — | `blocked` | Stopped after the account mutation |
| QA-24 | Growth behaviour with a 300+ character title | — | — | `blocked` | Never reached |
| DATE-01 | Overdue renders a relative word ("Yesterday") in `rgb(255,112,102)` with a calendar icon | scheduler-and-priority.md §9 | `format-task-date.ts` `formatTaskDate`/`describeDay`; `--td-date-overdue` | `built` | No calendar icon on the date badge itself (task-row-content.tsx) — the row's own metadata line carries no icons except comment/sub-task counts |
| DATE-02 | Completion **overrides** the date tone — a completed overdue row mutes to `rgb(204,204,204)` | scheduler-and-priority.md §9 | `format-task-date.ts` `formatTaskDate({ completed })`; `--td-date-muted` | `built` | |
| DATE-03 | Within the next 7 days renders the **weekday name only**, in `rgb(169,112,255)` | scheduler-and-priority.md §9 | measured in-app: a task 4 days out renders `Monday` in `rgb(169,112,255)` | `matched` | Driven side by side; weekday-name-only confirmed |
| DATE-04 | A recurring task appends ↻ after the date, same colour | scheduler-and-priority.md §7 | `format-task-date.ts` `formatTaskDate({ recurring })` | `built` | The literal `task.dateString` rule still renders as its own separate badge alongside the ↻'d date (task-row-content.tsx) — additive, not a replacement |
| DATE-05 | Upcoming day headings read `10 Sep ‧ Today ‧ Thursday`, separator U+2027; only today/tomorrow get a relative word | scheduler-and-priority.md §9 | `task-views.ts` `upcomingDayHeading()` (NAV-05); `format-task-date.ts` now shares the identical Today/Tomorrow words for `Task.date` itself | `built` | |
| DATE-06 | Row wording for a task due exactly today / tomorrow | pass2-2026-09-11.md §1 (Due today → `Today`, Due tomorrow → `Tomorrow`, both verified) | `format-task-date.ts:129` (`"Today"`), `:132` (`"Tomorrow"`) | `built` | **Unblocked** — pass 1 found no such task in the account; the account has since changed and pass 2 measured both, confirming the wording this ticket already implemented. Not `matched`: nobody drove the two apps side by side, and the row's own colour half is DATE-09, now falsified — see that row |
| DATE-07 | Row rendering of a date carrying a time | — | — | `blocked` | No such task exists — see DATE-10 |
| DATE-08 | Row rendering of a Deadline | — | — | `blocked` | **Deadline is Pro-only**; account is Free. `formatDay` (Task.deadline's own formatter) deliberately stays tone-less — see `format-task-date.ts`'s own header comment |
| DATE-09 | Row wording/colour for a Task due exactly today or tomorrow | pass2-2026-09-11.md §1 (Due today → `Today`, `rgb(37,184,76)` green; Due tomorrow → `Tomorrow`, `rgb(255,154,20)` orange; verified four times) | `format-task-date.ts`: `DateTone` (`:70`) now has its own `"tomorrow"` member, no longer folded into `"upcoming"`; `TONE_COLOUR` maps `today` → `--td-date-today` (`rgb(37,184,76)`) and `tomorrow` → `--td-date-tomorrow` (`rgb(255,154,20)`), both declared in `index.css`'s `[data-surface="todo"]` scope | `built` | **Issue #250 fixes the falsification this row previously recorded.** The guess (reusing `--td-date-upcoming` for Today, with no tone at all for Tomorrow) is replaced by the two measured colours above. **Dark theme only** — pass2-2026-09-11.md §1 is explicit these two colours are theme-dependent (Todoist's own "Todoist" theme reads `rgb(113,250,149)`/`rgb(255,180,83)` for the identical two states); DATE-12 tracks that still-open gap, untouched by this fix. Not `matched`: implemented and unit-tested (`format-task-date.test.ts`), but nobody has driven the two apps side by side in a real browser to confirm the computed colours — jsdom computes no layout or cascade, so that step is still outstanding |
| DATE-10 | Row wording for a Date carrying a time | — | `format-task-date.ts` `formatTaskDate` appends `", h:mm a"` after the resolved day word, e.g. "Tomorrow, 9:30 AM" | `built` | **Unverified** — DATE-07 is `blocked`, no timed Task was ever observed. A reasonable extension of the existing all-day wording, not a replay of a measured value |
| DATE-11 | Row wording for a Date more than 6 days out, or more than 1 day overdue ("further out / next year") | scheduler-and-priority.md §9 ("further out / next year," gap) | `format-task-date.ts` `describeDay` falls back to the plain absolute `formatDay`, toned `none` (upcoming side) or `overdue` (overdue side) | `built` | **Unverified**, but not unconstrained: the captured account's one completed, 9-days-overdue Task rendered as the plain "1 Sep," not a second relative phrase (scheduler-and-priority.md §9) — this fallback matches that data point even though the ticket that produced it never set out to establish this row |
| DATE-12 | Date colours are **theme-dependent** — the same Task's Today/Tomorrow colour differs between Todoist's own named themes | pass2-2026-09-11.md §1 (Today `rgb(113,250,149)` under the "Todoist" theme vs `rgb(37,184,76)` under Dark; Tomorrow `rgb(255,180,83)` vs `rgb(255,154,20)`) | `format-task-date.ts`'s `TONE_COLOUR` (`:72`) is a single static map with no theme parameter | `todoist-captured` | New row — no existing row tracked this axis. Nothing in meologue's `THEME-*` scope varies a Date colour by theme; any replica hard-coding one set of date colours (as `format-task-date.ts` does) only ever matches one Todoist theme, and closing this needs a decision on whether Todo's theme surface should carry per-theme date tokens at all |
| SCHED-01 | Anchored popover, 250×525px, radius 10px, background `rgb(38,38,38)`, border `1px solid rgb(61,61,61)` | scheduler-and-priority.md §1 | `task-schedule-popover.tsx`; `ui/popover.tsx`; `--td-popover-*` (index.css) | `built` | Dark theme; light untested. Measured live via ego-browser: `getComputedStyle` on the rendered card read back exactly 250×525px, `10px` radius, `rgb(38,38,38)` background, `1px solid rgb(61,61,61)` border, and the two-layer shadow below, byte for byte. **Issue #253 fixed the "reaching it" half this row previously recorded as broken.** The bottom sheet and the "Pick a date" button it gated the popover behind are both gone: the row's hover Date button, the detail view's Date attribute, the More-actions "Date…" item and the `T` shortcut now all anchor this same popover directly, via a per-site Radix `Popover.Trigger` (each real ref-forwarding — `task-row-content.tsx`'s own doc comment on why that specifically matters). **Anchoring is now measured, and one entry point is broken.** `verification-2026-09-11.md`: all four entry points were driven in a real browser and the popover's rect was read against its trigger's rect — row hover Date button, More-actions "Date…", the `T` shortcut and the detail view's Date attribute each anchored directly under their own control, none at the viewport origin, at 250×525 and 10px radius. Set date, clear date and set recurrence were exercised from two different entry points. **But the More-actions "Date…" item does not reliably open it by MOUSE: measured 2 of 10 attempts, against 3 of 3 by keyboard, deterministic by row position (bottom rows open, upper rows never do) rather than intermittent — filed as issue #255 with the evidence and two reverted fix attempts.** Stays `built`, not `matched`: three of four entry points behave, one does not, and a row cannot claim a match while a reader clicking a menu item mostly gets nothing |
| SCHED-02 | Quick options in order: Today `Thu` · Tomorrow `Fri` · This weekend `Sat` · Next week `Mon 14 Sep` | scheduler-and-priority.md §2 | `task-schedule-popover.tsx`'s `QuickOption` list | `built` | Next week shows a full date, others a weekday. Verified in-app on the reference instant (Thu 10 Sep 2026): hints read exactly Thu/Fri/Sat/Mon 14 Sep. Status left unchanged: pass2-2026-09-11.md §5 records that Todoist's own slots 3/4 changed (`This weekend`/`Next week` → `Next week`/`Next weekend`) between 10 and 11 Sep — a live-product change, not a code defect, and restatusing depends on an unmade decision (chase Todoist's live product, or pin to a capture date) |
| SCHED-03 | "No Date" appears **only once a date is already set** | scheduler-and-priority.md §2 | `task-schedule-popover.tsx` (`dateDay !== null` guard) | `built` | |
| SCHED-04 | A "Type a date" input accepts dates and recurrence phrases and previews the resolution above the quick options | scheduler-and-priority.md §3 | `task-schedule-popover.tsx`'s `resolveSchedulePreview`, reusing `@meologue/core`'s `parseQuickAdd`/`firstOccurrence` | `built` | Preview shows the resolved date and a real Task count (`datesWithTasks`, threaded from the caller's own task list) — not a fabricated "No tasks" placeholder. The "→ Forever"/"→ Ends" suffix is unverified beyond the unbounded case: a bounded phrase's own resolved end date is not re-derived (would require exposing `../recurrence/engine.ts`'s private `resolveBoundDate`), so it renders the generic "→ Ends" rather than a specific date |
| SCHED-05 | Picking a date **writes the phrase back into the title** as a highlighted recognition token | scheduler-and-priority.md §3 | — | `divergent` | Deliberately not reproduced, and re-statused from `todoist-captured` because "captured, nothing built" misdescribes a row nobody intends to build: that behaviour is Todoist's Quick Add typing a still-unsaved title at the same time a date is picked. This popover schedules an already-created Task with no title being composed alongside it, so there is no title to write into. The *parsing* mechanism is still shared (SCHED-04's own reuse of `todo-quick-add-recognition.ts`'s underlying `parseQuickAdd`/`firstOccurrence`), which is the part of this row that actually applies here — the DOM side-effect does not |
| SCHED-06 | Calendar week starts **Monday** | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `<Calendar weekStartsOn={1}>`, `formatters.formatWeekdayName` | `built` | Header reads `M T W T F S S`, verified in-app |
| SCHED-07 | Today carries **no `aria-current`** and no ring — only bold and `rgb(226,106,96)` | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `classNames.today`; `--td-calendar-today` | `built` | Deliberate; do not "fix". Verified in-app: today's cell carries no `aria-current`, bold text, `rgb(226,106,96)`. **This row's own test was true on exactly one day.** It asserted `data-today="true"` on 2026-09-10 — the capture instant — through an attribute react-day-picker derived from the *real* system clock, while everything else in the popover derives from the injected `now`. The suite went red the morning after the capture and would have stayed red permanently; `today={now}` now threads the same clock through DayPicker. A date-pinned test that reads one value off the wall clock is pinned in name only |
| SCHED-08 | Selected day is a filled circle, `rgb(222,76,74)`, 24px, radius 12px | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `classNames.selected`/`day_button` (`size-6 rounded-full`); `--td-calendar-selected` | `built` | Verified in-app |
| SCHED-09 | Days carrying tasks get a 3×3px dot via `::before`, no extra DOM node | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `modifiersClassNames.busy`; `--td-calendar-busy-dot` | `built` | Verified in-app against real seeded Tasks |
| SCHED-10 | Weekends dim to `rgb(204,204,204)`, independent of today/busy | scheduler-and-priority.md §4 | `task-schedule-popover.tsx`'s `modifiersClassNames.weekend` (reuses `--muted-foreground`, which already resolves to this exact value under this surface's dark theme) | `built` | |
| SCHED-11 | Time opens a second dialog with start time, duration and timezone, defaulting to "Floating time" | scheduler-and-priority.md §5 | — | `todoist-captured` | Matches our floating-time glossary rule. Out of this ticket's scope — the existing "Add a time" toggle/`<input type="time">`, in `task-schedule-popover.tsx` since issue #249 (this row's own citation was already stale — #253 confirms the toggle has no presence in `task-schedule-sheet.tsx` at all any more, that file's Date section having been removed outright), is untouched, not the dedicated dialog described here |
| SCHED-12 | Duration is Pro-gated | scheduler-and-priority.md §5 | — | `divergent` | We removed Duration deliberately (issue #179) |
| SCHED-13 | The Deadline picker's own UI | — | — | `blocked` | **Pro-only paywall** |
| SCHED-14 | The dedicated Repeat dialog's contents | pass2-2026-09-11.md §6 — `role="menu"`, 282×206, radius 10px, `rgb(40,40,40)`, `1px solid rgb(61,61,61)`, `rgba(0,0,0,.12) 0 0 8px`, z-index 1000; items in order: Every day, Every week on Friday, Every weekday (Mon–Fri), Every month on the 11th, Every year on September 11th, Custom… | — | `todoist-captured` | Never previously opened; now fully captured. Still unbuilt — meologue has no dedicated Repeat dialog, only the recurrence phrase typed into the scheduler's "Type a date" input (SCHED-04) |
| PRI-01 | Picker swatches: P1 `rgb(209,69,59)`, P2 `rgb(235,137,9)`, P3 `rgb(36,111,224)`, P4 `rgb(102,102,102)` | scheduler-and-priority.md §10a | All four literals exist as `--td-priority-picker-*` tokens (`index.css`), mapped by `lib/task-priority-colors.ts` `priorityPickerColour` and painted as the picker's swatches in `task-command-menu.tsx` | `built` | **Status was stale** — landed with #223's palette and never reflected here. Four confirmed genuinely distinct. See PRI-03 for the one swatch whose rendering is contested |
| PRI-02 | Internal `data-value` is **inverted** from the label — P1 is 4 | scheduler-and-priority.md §10a | — | `todoist-captured` | Our store already inverts identically |
| PRI-03 | P4 is pre-selected and is the invisible default — a transparent ring, not a grey one | scheduler-and-priority.md §10d | "Pre-selected" is built: an untouched Task stores `priority: 1` → ui 4, and `task-command-menu.tsx` marks it `aria-pressed`. The **transparent** ring is not: the P4 swatch paints `--td-priority-picker-4` = `rgb(102,102,102)`, and a P4 row's checkbox paints `--td-checkbox-ring-default` = `rgb(169,169,169)` | `blocked` | **Two reference documents disagree and this row cannot be closed until they are reconciled.** `scheduler-and-priority.md` §10c/§10d measured a transparent P4 ring; `row-and-detail.md` §1 measured a grey `rgb(169,169,169)` one — and ROW-03 is **already `matched`** on the grey value. They may both be right about different surfaces (picker swatch vs row checkbox) or one may be a mis-read. Re-statused from `todoist-captured` to `blocked` because the blocker is the corpus, not the code: do not "fix" either side before a capture pass settles which surface shows which |
| PRI-04 | The Quick Add pill colours only the **flag icon**; its `P1` text stays neutral grey | scheduler-and-priority.md §10b | — | `todoist-captured` | |
| PRI-05 | The row checkbox ring red `rgb(255,112,102)` is **not** the picker red `rgb(209,69,59)` | scheduler-and-priority.md §10c | `task-priority-colors.ts` `priorityColour` (`--td-priority-row-1`) vs `priorityPickerColour` (`--td-priority-picker-1`) | `built` | Two surfaces, two values — don't unify into one token. Implemented by issue #223, before this ledger row was ever pointed at code; issue #224 only updated this row to say so |
| PRI-06 | Row rendering for P2 and P3 | scheduler-and-priority.md §10c | — | `blocked` | No P2/P3 task exists in the account |
| ROW-01 | Row height 59px, zero padding on the row itself | row-and-detail.md §1 | measured in-app: 59px | `matched` | Driven side by side |
| ROW-02 | Divider is `border-bottom: 1px solid rgb(61,61,61)` with **0px left inset** — full list width | row-and-detail.md §1 | measured in-app: `1px solid rgb(61,61,61)` on the row box | `matched` | First probe read 0px — it measured the `<li>` where the divider sits on the row box. The code was right and the probe was wrong |
| ROW-03 | Checkbox: 24×24 hit box around an 18×18 visible ring, `1px solid rgb(169,169,169)` (2px at P1), tinted by priority | row-and-detail.md §1; pass2-2026-09-11.md §2 (ring width: P1 2px, P4 1px) | measured in-app: 18x18 in a 24x24 hit box; ring `rgb(169,169,169)`, `rgb(255,112,102)` at P1; `task-row-content.tsx:330-362`. **Issue #250 fixed the width axis**: the `box-shadow` spread is now `uiPriorityOf(task.priority) === 1 ? "2px" : "1px"` (`task-row-content.tsx`, the checkbox `<input>`'s own `style`), no longer a flat `1px` for every priority | `built` | **Still not `matched` — one axis fixed, one still open.** This row's own "What `matched` requires" section demands behavioural, structural AND dimensional agreement. Dimensionally, ring width now varies by priority as measured (`task-row.test.tsx`'s own P1/non-P1 tests pin it) — real-browser pixel confirmation is still outstanding, jsdom reads the inline style but computes no cascade. Structurally, the divergence this row already carried is untouched by issue #250 (styling-only): Todoist uses `<button class="task_checkbox" role="checkbox" aria-label="Mark task as complete">`; meologue uses `<label><input type="checkbox" readOnly aria-label={task.content}>` — different element, different role, different accessible name. **Caveat carried forward:** the `rgb(169,169,169)` ring value itself is contradicted by `scheduler-and-priority.md` §10c/§10d, which measured a *transparent* ring for P4 — see PRI-03, `blocked` on exactly that |
| ROW-04 | Hover reveals drag handle, then Edit · Date · Comment · More, each `opacity` 0→1; the row background does **not** change | row-and-detail.md §1 | measured in-app: every affordance `opacity: 0` at rest | `matched` | Initially failed: variant classes built by string interpolation, which Tailwind's text scanner never sees, so no CSS existed and every control showed on every row |
| ROW-05 | Long titles **wrap** to 4 lines via `-webkit-line-clamp: 4`, they do not truncate to one | row-and-detail.md §1 | measured in-app: `-webkit-line-clamp: 4`, title 14px/21px | `matched` | Driven side by side |
| ROW-06 | Markdown in a **title** stays literal | row-and-detail.md §1 | `task-row-content.tsx`: `task.content` interpolated as plain text, never parsed | `built` | |
| ROW-07 | A Description renders as **real HTML from markdown**, previewed as a first line beneath the title | row-and-detail.md §1 | `task-row-content.tsx`: first `\n`-delimited line of `task.description`, rendered via `inlineProse`, truncated to one line | `built` | Inline markdown only (bold/italic/code/links) via `inlineProse`, not the block-level `entryProse` (lists, multiple paragraphs) — a first-line preview by definition can't show a second block anyway |
| ROW-08 | Comment count is an icon plus number, linking to `?intent=reply` | row-and-detail.md §1 | `task-row-content.tsx`: `MessageSquare` icon + count | `built` | **Divergent from the reference in one respect, not silently**: this is a plain badge, not an `<a href="…?intent=reply">` — clicking it does nothing on its own (the separate Comment hover action opens the detail view instead). Not touched by issue #224; carried over unchanged from issue #180 |
| ROW-09 | Metadata blocks are adjacent flex children with a gap — no separator glyph between date and comments | row-and-detail.md §1 | `task-row-content.tsx`: one `flex flex-wrap items-center gap-x-2` line for every badge, old and new alike | `built` | Extended to the newly-added Labels/Project/sub-task-count badges under the identical rule — see ROW-10 for why their very presence/order is still unverified |
| ROW-10 | Labels, the Project a Task lives in, and a sub-task count all render on the row, as further adjacent flex children with no separator | — | `task-row-content.tsx`: `LabelBadge` per resolved Label, Project name via `projectNameFor`, sub-task count via `ListTree` + number | `built` | **Unverified.** ROW-09's own note already conceded the gap: none of the 7 captured Inbox tasks carried a Label, a cross-Project view, or a sub-task, so neither their presence, their order relative to Date/Priority/comments, nor any icon choice for them was ever observed. This row exists so that gap has a name rather than being silently folded into ROW-09's "matched" claim |
| ROW-11 | Hovering a row mounts Complete · Edit · Date · Comment · More, in that order; clicking the **Edit** pencil activates the shared `tiptap ProseMirror` editor in place — no dialog, no URL change | driven directly, issue #225's own follow-up | driven in-app: Edit pencil opens `contenteditable role=textbox aria-label="Task name" class="ProseMirror tiptap"`, URL unchanged, rename persisted | `matched` | Matched on gesture, markup and URL behaviour. One structural divergence kept: Todoist's editor sits OUTSIDE the `li` (it swaps the row for an edit form); ours renders inside it. The double-click gesture first built here was an invention and was replaced once the pencil was actually driven in Todoist |
| ROW-12 | The row's hover controls (Edit/Date/Comment/More) are **not in the DOM at rest at all** — Todoist mounts them on hover rather than rendering them permanently | driven directly, issue #225's own follow-up | `task-row-content.tsx`: all four rendered unconditionally, revealed via `opacity-0`/`group-hover:opacity-100` (ROW-04's own `matched` measurement, which read the computed style but not whether the elements existed at rest) | `divergent` | Deliberate accessibility choice, not an oversight: permanently-mounted controls stay reachable by keyboard `Tab`/focus without a hover event ever firing, which a reader with no pointer needs. Mount-on-hover would make them unreachable by keyboard alone unless a second, hover-independent path were built — not worth trading away for DOM parity with no behavioural upside. |
| ROW-13 | Today's Due-today rows render a redundant date badge; Todoist omits the date control from the DOM entirely on such a row, rather than hiding it | pass2-2026-09-11.md §3 ("Today view omits the date control entirely — the control is absent from the DOM, not hidden") | **Issue #250 built the suppression path.** `TaskRowContent` gained a `suppressDateBadge` prop (`task-row-content.tsx`), threaded through `TaskRow` and set unconditionally on every row in `today-view.tsx`'s own `dueToday` section (its Overdue section leaves it unset — an overdue row's date is never redundant); when set, the date badge is omitted from the render entirely, not merely hidden with CSS, matching the DOM-absence pass2 measured | `built` | `today-view.test.tsx`'s own test pins the DOM absence side by side with an overdue row that still renders its date. Not `matched`: nobody has driven the two apps side by side in a real browser — this is a jsdom/RTL confirmation that the element is absent from the tree, the same axis pass2 measured, but not a live comparison |
| ROW-14 | The main task list segregates completed Tasks into a collapsed `<details>` below the active list; Todoist interleaves completed rows inline within the same list | pass2-2026-09-11.md §3 (measured: Inbox contains 7 active + 6 completed = 13 `li[data-item-id]` in one list, each `aria-checked`) | `completed-tasks.tsx` renders a separate collapsed `<details>` (`:36-61`); `task-row-content.tsx:333` hardcodes `checked={false} readOnly` on the active row's own checkbox | `built` | New row — structural, not cosmetic (audit-2026-09-11.md §4 item 6): the active row's checkbox can never render checked, so that list shape structurally cannot hold a completed row even before considering where it's placed. **Issue #250 (the three cheap, styling-only fixes ROW-15/ROW-13/ROW-03 above) deliberately left this row untouched** — its own acceptance criteria never asked for the structural merge, and closing it means rebuilding the active/completed list as one interleaved, `aria-checked` collection rather than a styling change to either half. Recorded here explicitly so the deferral reads as a decision, not an oversight |
| ROW-15 | A completed row's title and date carry two distinct colours, and the title always strikes through; meologue paints only the title muted and gates strikethrough behind a Settings default | pass2-2026-09-11.md §1 (completed title `rgb(128,128,128)` + `line-through`; completed date `rgb(204,204,204)`) | **Issue #250 fixed both axes.** `index.css`'s `[data-surface="todo"]` scope now re-points `--checked-list-text-decoration`/`-color` unconditionally (`line-through` / `rgb(128,128,128)`), overriding whichever of the four `data-completed-style` values `<html>` carries — the same "RE-POINT, DON'T RENAME" move this scope already makes for `--background` et al., so only Todo's own completed rows (`.completed-task-text` consumers: `completed-tasks.tsx`, `task-detail-view.tsx`, `filter-view.tsx`, `task-search-page.tsx`) always strike through; History/Composer, outside this scope, keep obeying the Settings option unchanged. `completed-tasks.tsx` also now renders the Task's own date beside the title (previously dropped entirely), through `formatTaskDate({ completed: true })` — DATE-02's existing "completion overrides tone" rule already resolves it to `--td-date-muted` = `--muted-foreground` = `rgb(204,204,204)` in this scope's Dark block, with no new token needed for that half | `built` | `completed-tasks.test.tsx` pins the date now rendering (and staying absent when a Task has none); the strikethrough/colour override is a CSS re-point jsdom's own lack of a cascade can't pin from a test, the same limit `format-task-date.test.ts`'s own colour assertions already accept for every other `--td-*` token. Not `matched`: real-browser confirmation of the computed `rgb(128,128,128)` + `line-through` is still outstanding |
| DET-01 | Opening a task changes the URL to `/app/task/{slug}-{id}`; Escape returns | row-and-detail.md §2 | `lib/task-detail-route.ts` `taskDetailPath`/`taskIdFromParam`, routed in `App.tsx`; Escape reaches `closeTaskDetail()` because the Radix `Dialog.Content` does not override `onEscapeKeyDown` | `built` | **Status was stale** — the route and the Escape path both predate this ledger. One deliberate divergence: our prefix is `/todo/task/…`, not Todoist's `/app/task/…`, because Todo is a Destination inside this app rather than the app itself (that module's own header comment) |
| DET-02 | The detail title at rest is a **non-editable `div.task_content`** — the same display component the row uses, not Quick Add's editor | row-and-detail.md §2 | `task-detail-view.tsx` (`editingTitle`) | `built` | Display and edit are split. A `<button>`, not a bare `<div>`, is meologue's own choice for how "activate" is reached without a pointer — the reference never had to specify since only a click was driven. |
| DET-03 | The title carries the hint "Activate to edit the task name" | row-and-detail.md §2 | `task-detail-view.tsx` (`titleHintId`, `aria-describedby`) | `built` | Verbatim text, on a visually-hidden `<span>` the display button's own `aria-describedby` points to. |
| DET-04 | What the title becomes **once activated** for editing | — | — | `blocked` | Would have meant typing into a real task; the open question for #225 |
| DET-05 | The detail dialog is `data-testid="task-details-modal"` | keyboard.md §1 | `task-detail-view.tsx:967` | `built` | **Rot corrected** — this row read "nothing built" while `task-detail-view.tsx:967` already carries `data-testid="task-details-modal"` and cites DET-05 by name in its own comment |
| KBD-01 | The overlay lists **80 shortcuts across 8 sections**: General 13, Quick Add 11, Navigate 15, Edit task 17, Add task 5, Sub-task 3, Projects 11, Calendar/Upcoming 5 | keyboard.md §1 | — | `todoist-captured` | Transcribed, not driven |
| KBD-02 | `?` opens the shortcuts overlay | keyboard.md §1 | `todo-keyboard-shortcuts-overlay.tsx`, wired by `use-todo-keymap.ts` | `built` | Issue #228. Not `matched` — nobody drove Todoist and meologue side by side for this row, only meologue itself (this ticket's own browser verification). Our overlay renders far fewer than 80 rows: only bindings `todo-keymap.ts` actually wires (that module's own header comment lists what's deliberately absent and why). |
| KBD-03 | **Both** arrow keys and `j`/`k` move focus between rows | keyboard.md §2 | — | `todoist-captured` | Verified via `document.activeElement`. Not touched by issue #228 — row-to-row focus movement is a separate, larger feature (this app's rows currently reach each other by `Tab` order alone) than the single-Task/navigation bindings this ticket wires. |
| KBD-04 | Focus **wraps** at both ends, and the cycle includes the "Add task" button and completed rows | keyboard.md §2 | — | `todoist-captured` | Verified |
| KBD-05 | The focus ring is an **inset box-shadow** `rgb(23,91,194) 0 0 0 1px` plus a `rgba(255,255,255,0.14)` background tint — the CSS `outline` property is unset | keyboard.md §2 | — | `todoist-captured` | Verified in Todoist. Unbuilt here: `--td-focus-ring` is declared in `index.css` with **zero consumers**, and focus is still drawn by Tailwind's `focus-visible:ring-*`/`outline` utilities — so this row's "`outline` is unset" clause is currently contradicted by our own CSS. Third instance of the dead-token trap; see QA-16 and QA-20 |
| KBD-06 | The full 80-key map, key by key | keyboard.md §1 | `@/lib/todo-keymap.ts` (`TODO_KEY_BINDINGS`) | `built` | Issue #228 wires a small, named subset — Quick find (`/`, `f`, `⌘K`), `?`, `.` (More actions), `⌘E` (Edit), `T`/`⇧T` (Date/Remove date), `D`/`⇧D` (Deadline/Remove deadline), `Y` (Priority), `⌘⌫`/`⇧Delete` (Delete), `⇧G` (open Task in its Project), and `G`-then-`I`/`T`/`U`/`P`/`V` (go to Inbox/Today/Upcoming/Projects/Filters). **Issue #253 split `T` off from `D`/`Y`:** `D`/`Y` still open the combined schedule sheet (Deadline/Priority, `task-schedule-sheet.tsx`), but `T` now opens the row's own anchored `TaskSchedulePopover` instance via `OPEN_SCHEDULE_EVENT` (todo-keymap.ts) instead — this row's own former wording ("Y … opens the same combined schedule sheet as T/D") is corrected here rather than left to misdescribe the binding table it cites. Every other key stays `todoist-captured`, unimplemented. **Overclaim corrected:** this note used to say `todo-keymap.ts`'s own header comment "names each key left out and why." It does not — `Z`/`⌘Z` (undo), `⌘A`, the arrow keys, `Esc` and `⌘⌥0` are all absent from `TODO_KEY_BINDINGS` (`todo-keymap.ts:95-216`) with no acknowledgement anywhere in the file. That silence is why `CMT-05`'s missing undo binding went unflagged for as long as it did. `V` ("Move to…") specifically was left out even though a menu item already invites it: no controlled way exists to open that one Radix submenu pre-expanded without a larger refactor. Not `matched` for the same reason as KBD-02 — meologue-only verification, not a side-by-side drive. |
| DET-06 | Activating the title swaps in `tiptap ProseMirror` editors labelled "Task name" and "Description" — **the same component as Quick Add** | lifecycle.md, quick-add.md | driven in-app: same editor markup in the row's inline rename; detail view uses the identical component | `matched` | Matched for row and detail. **Not yet in [meologue's own] Quick Add** — it still runs the older backdrop highlighting, deliberately, until #226 lands a decoration plugin; converting it early would delete working recognition for a whole ticket |
| DET-07 | Recognition fires in the **detail** title exactly as in Quick Add — same span, padding and match id — and a recognised phrase resolves on save, exactly as it does for a new Task | lifecycle.md; `rename-capture-2026-09-11.md` (both of Todoist's own rename surfaces driven directly) | `task-detail-view.tsx:367-391,578-580` (highlighting), `task-title-commit.ts`'s `commitTaskTitle` (resolving), `todo-page.tsx`'s and `composer-page.tsx`'s own `commitRename` wrappers, `task-row-content.tsx:387-394` (the inline row editor, which gained the identical highlighting plugin the same ticket) | `built` | **Restatused (issue #247), and the reference is no longer silent.** Highlighting was already built and cited here (#225/#226). Saving was not: until now, `saveEditing`/`commitTitle` both handed the raw typed text straight to `onRename`, which reached `renameTask` directly — a phrase rendered a span but never touched a Task field, and the row editor had no recognition plugin attached at all. #247 moved `onRename` one layer up on both surfaces (the page's own `commitRename`) so it resolves through the identical `taskFieldsForRename`/`commitTaskTitle` door Quick Add's `addTask` already goes through, only ever setting a field a phrase actually resolved (`quick-add-task.test.ts`'s `taskFieldsForRename` suite, `task-title-commit.test.ts`, and `todo-page.test.tsx`/`composer-page.test.tsx`'s new rename cases). `rename-capture-2026-09-11.md` closes the gap the pre-#247 code cited as its own reason for staying unparsed: both Todoist rename surfaces resolve a phrase and strip it, matching the shape this row now builds — but that capture did not exercise a failed phrase or a phrase-free rename, so this app's own conservative "only ever set a field that actually resolved, never clear one the reader didn't touch" is still an unevidenced choice on our side, not a measured match. One further, unbuilt divergence that capture found: Todoist's own detail-dialog rename raises a toast naming the field it changed ("Date updated to Tomorrow", with Undo); meologue raises none. Still not `matched` — Todoist was driven for this capture, but nobody has driven meologue's own two rename surfaces side by side against it in the same session |
| DET-08 | While editing, the properties panel's Date control does **not** reflect an in-title recognition until save — unlike Quick Add's footer, which fills live | lifecycle.md | `task-detail-view.tsx` | `built` | The two surfaces stay unlinked exactly as observed: the Date attribute row still reads off `task.date`, never off the title editor's own live state, so there is structurally nothing for it to reflect early. Vacuous until #226 gives the title something to recognise, but the separation itself is real and deliberate. |
| DET-09 | Editing is **task-wide**: clicking the description puts title and description into edit together, sharing one Cancel/Save | lifecycle.md §1 | `task-detail-view.tsx`'s `editing`/`focusField`/`saveEditing` (issue #229) | `built` | One shared form, one Cancel/Save pair, replacing the pre-#229 per-field `editingTitle`/`editingDescription` blur-commit shape. Not yet `matched` — driven in meologue only this ticket, not side by side with Todoist |
| DET-10 | Clicking a generic part of the edit form focuses the **title**, not the description | lifecycle.md §1 | `task-detail-view.tsx`'s `focusField` | `built` | Reproduced via the two entry points this view actually has: the description's own click target (the pill, or the rendered block) sets `focusField: "description"`; every other entry point (today, only the title's own button) sets `"title"` |
| DET-11 | The Description placeholder reads exactly "Description"; there is **no formatting toolbar** anywhere | lifecycle.md §1 | `task-description-editor.tsx`'s `TaskDescriptionEditor` default `placeholder` | `built` | No toolbar element anywhere, matching the reference |
| DET-12 | Description markdown renders **live as input rules** — bold, bullet list, inline code | lifecycle.md §1 | `task-description-editor.tsx` (`descriptionSchema`, `strongInputRule`/`codeInputRule`/`bulletListInputRule`) | `built` | A bespoke, minimal ProseMirror schema — not `entrySchema` — for bundle-budget reasons (that file's own header comment). Covers exactly the three named constructs; a numbered list, heading or other markdown construct stays literal text rather than crashing |
| DET-13 | A bare URL in a description is **rewritten on save** to the fetched page title | lifecycle.md §1 | — | `divergent` | Network-dependent; deliberately not implemented (this ticket's own brief). `task-description-editor.tsx` recognises no URL at all |
| CMT-01 | Comments submit on **Ctrl/Cmd+Enter or the "Comment" button** — Enter and Shift+Enter both insert a newline | lifecycle.md §2 | `task-detail-view.tsx`'s `CommentComposer` `onKeyDown` | `built` | Opposite of Quick Add, kept deliberately unmerged |
| CMT-02 | Comment markdown renders like the description's, but a bare URL keeps its text and gains an unfurl card | lifecycle.md §2 | `entryProse` (already shared with the description) | `built` | The unfurl card itself is not built (no network fetch anywhere in this view); the "keeps its text, no rewrite" half already matches since neither surface rewrites a URL |
| CMT-03 | Comment edit, delete and their confirmation wording | lifecycle.md §2 | `task-detail-view.tsx`'s `ConfirmDialog` (title "Delete comment?", body "This comment will be permanently deleted.") | `built` | Ours used to delete with no confirmation at all |
| CMT-04 | Undo toast reads exactly **"1 task completed"** with an "Undo" button and a Close, in `role="alert" aria-live="polite"` | lifecycle.md §3 | `todo-page.tsx:390-395` — a completion toast already ships, with an Undo action | `divergent` | **Rot corrected** — this row read "nothing built" while `todo-page.tsx:390-395` already fires a toast with an Undo button on every completion. Not a match, though: wording is `Completed "<task name>"` (task-specific), where Todoist's is the task-agnostic, count-based `1 task completed`. Recorded as `divergent` rather than `built` because the two have already been compared and differ — but this is a discovered gap, not a deliberate decision, and still needs one |
| CMT-05 | The toast auto-dismisses after **6–8 seconds**, and Ctrl/Cmd+Z also undoes a completion | lifecycle.md §3 | same toast as CMT-04 (`todo-page.tsx:390-395`) | `divergent` | **Rot corrected** — this row read "nothing built" while the toast above already exists. It diverges on both halves of this row: lifetime is ~4s (sonner's own default; no `duration` override anywhere in the codebase), against Todoist's measured 6–8s; and `Ctrl/Cmd+Z` is genuinely **not bound** — no `mod+z` entry exists anywhere in `todo-keymap.ts:95-216`. As with CMT-04, `divergent` here marks "compared and found different," not a design choice |
| CMT-06 | Activity wording: `You added {task}`, `You completed {task}`, `You uncompleted {task}`, `You commented {content} on {task}`, `You added a description {content} to {task}`, `You changed the description of {task} to {content}`, `You removed the description {content} from {task}`, `You deleted a comment from {task}`, `You changed the name of {task}` | lifecycle.md §4 | `format-event.ts`'s `describeEventLine` (issue #229) | `built` | **Overclaim corrected: only 5 of 9 templates are verbatim, not all nine.** The four carrying `{content}` (`format-event.ts:258` comment, `:377` removed-description, `:387` added-description, `:398` changed-description) wrap the content in literal quotation marks (`` `"${content}"` ``); Todoist's own DOM instead renders a clickable, unquoted content-preview chip in that position. The rename showing only the new name is correct, and Description changes were previously not recorded as an Event at all (`use-tasks.ts`'s `setDescriptionMutation`) until this ticket fixed it. Every other event type (moved, date/deadline/priority/labels, Project/Section) is untouched — not part of this row |
| CMT-07 | The activity list has **no filters** and ends with "That's it. No more history to load." | lifecycle.md §4 | `activity-feed.tsx`'s `completedOnly` narrowing, driven by a caller-owned toggle in `todo-page.tsx` | `divergent` | **We ship the opposite of this row, on purpose.** Issue #184 gave the feed a completions filter as a designed feature, so "no filters" is not a gap to close — it is a choice already made against it. Re-statused from `todoist-captured`, which wrongly implied somebody still owed the work. The terminal line ("That's it…") is genuinely absent and genuinely unbuilt; it is too small to carry a row of its own, so it is recorded here rather than promoted |
| CMT-08 | Italics, strikethrough, headings, numbered lists, blockquotes, fenced code blocks | — | — | `blocked` | Not exercised in the write session |
| THEME-01 | Todoist's light-theme palette, type and density | pass2-2026-09-11.md §10 | `index.css`'s `[data-surface="todo"]` base block (issue #223) — meologue's own light design | `divergent` | **Closed as a deliberate divergence (user decision, 2026-09-11): meologue's light theme is its own design, not a replica, and light-mode parity rows are no longer tracked.** This is not a capture task left open — pass 2 eliminated every candidate cause: all 8 named themes plus an undocumented 9th render dark; the Sync theme and Auto Dark Mode toggles, a forced `prefers-color-scheme: light`, Chromium's own force-dark override (`Emulation.setAutoDarkModeOverride`), the Dark Reader extension (verified 0/0/0 on all three contamination signals) and the browser's launch flags were each ruled out; and macOS itself was switched to Light appearance and confirmed inert. No light palette is reachable on this account's Free plan tier, so no light-mode row could ever read `matched` — tracking them would promise a comparison that cannot be made |
| THEME-02 | Dark palette: content pane `rgb(31,31,31)`, sidebar/body ground `rgb(38,38,38)` — the secondary surface reads **lighter** than the page | scheduler-and-priority.md, measured directly | measured in-app: sidebar `rgb(38,38,38)`, content `rgb(31,31,31)` | `matched` | Dark only. Initially failed: the sidebar painted nothing and inherited the content colour — caught by looking at a screenshot, not by any test |
| THEME-03 | Borders `rgb(61,61,61)`, muted text `rgb(204,204,204)`, focus ring `rgb(23,91,194)` | corpus | `index.css` | `built` | |
| THEME-04 | Type scale: row 14px/21px, chrome 13px, Quick Add title 16px/23px | row-and-detail.md, quick-add.md | `index.css` `--td-*-font-size` | `built` | Three sizes this app never had to distinguish |
| THEME-05 | The system font stack, not Geist, inside Todo | quick-add.md § fonts | measured in-app: resolves to `-apple-system, "system-ui", …` | `matched` | Initially failed silently — `@theme inline` bakes `--font-sans`'s literal into the `font-sans` utility, so re-pointing the token was inert while every test stayed green. Fixed with a direct `font-family` on the scope |
| NAV-08 | The chrome/sidebar renders at 13px, smaller than the row text | row-and-detail.md, quick-add.md § fonts | measured in-app: 13px | `matched` | |
| NAV-09 | Content column: Todoist left-aligns an **800px** column beside a **280px** sidebar and puts the view name in it as a large heading (`<h1>`, 26px / weight 700 / 35px line-height, left-aligned flush with the column, **no separate app bar**); ours now caps the same column at **800px** above the 900px wide-layout breakpoint and renders the view name as an in-column `<h1>`, Back and the Sync dot relocated into that row, no separate app bar | pass2-2026-09-11.md §8 | `shell.tsx` (`columnWidthClassName` — the 800px cap, `w-[97%] md:w-[85%] min-[900px]:w-full min-[900px]:max-w-[800px]`; `hideAppBar` — the in-column heading row); `todo-page.tsx` (`todoHeading`, the view→heading mapping: Inbox/Today/Upcoming/Projects/Search/Activity/Filters & Labels/Labels, plus a Project's or Filter's own resolved name) | `built` | **Built against issue #254, not yet `matched`.** jsdom lays out nothing, so `shell.test.tsx`/`todo-page.test.tsx` pin the classes and the mapping, not the pixel values — 800px and 26px/700/35px are still unverified on a real screen; nobody has driven the two apps side by side. **One divergence, deliberate and recorded in ADR 0019's own amendment:** meologue's cap engages at **900px**, not Todoist's own sidebar-driven breakpoint — reusing the existing wide-layout breakpoint (where Todo's sidebar and bottom bar already change) rather than adding a second transition. **A consequence, also recorded there:** Back and the Sync dot now scroll away with the column instead of staying pinned in a fixed bar — matching Todoist's own behaviour, but a real loss on a long Inbox. Previously: **Estimate replaced with measured values.** This row previously guessed "~750px" with no measurement; pass 2 measured the content column at 800px (left 468 → right 1268 at a 1470px viewport), the sidebar at 280px, and the view-name heading at 26px/700/35px — and confirmed there is no separate app bar at all, the view name being the in-column heading itself. **Citation corrected (carried forward):** this row previously cited `pairs/NAV-01-meologue.png`, a file that does not exist, in a directory that does not exist, under another row's ID — see this file's own note on `pairs/` above |
| NAV-10 | Todoist's add-task affordance is a subtle "+ Add task" row at the **end of the list**, with Quick Add as an overlay; ours is a bordered input with an "Add" button at the **top** | quick-add.md § Quick Add chrome | screenshot | `built` | **Position and weight fixed by issue #252.** `add-task-form.tsx`'s render moved from before Inbox/Today/a Project's own list to just after it (`todo-page.tsx`), and the field's resting border was dropped (`EDITOR_BOX_CLASSES`'s `border-input` → `border-transparent`). Not `matched`: the 14px/`rgb(128,128,128)` figures are a real-browser measurement nobody has taken against ours yet (jsdom computes no layout), and see NAV-12 for the part of this row #252 deliberately left unbuilt — the click-to-reveal composer, still an overlay-free always-mounted field here |
| NAV-01 | Sidebar order: Add task · Search · Inbox / Today / Upcoming / Filters & Labels with counts · **Reporting** · "My Projects" tree | row-and-detail.md, live sidebar; pass2-2026-09-11.md §9 | `todo-sidebar.tsx` | `built` | **Corrected against pass 2's live sidebar.** This row's order previously omitted a **Reporting** row (→ `/app/activity`) that the live sidebar carries, and read the projects heading as "Projects" where it actually reads **"My Projects."** Live order in full: Add task · Search · Inbox · Today · Upcoming · Filters & Labels · Reporting · My Projects, plus a "Dictate tasks with Ramble" button beside Add task. `todo-sidebar.tsx` has neither Reporting nor the "My Projects" wording — see the new `NAV-11` row for the Reporting/Activity gap specifically |
| NAV-02 | The sidebar renders only inside `/todo/*`, at ≥900px, in the pane the shell already has | ADR 0076 | `chat-shell-layout.tsx` | `built` | No second pane, divider or width mechanism. **Citation corrected (issue #248):** this row cited "ADR 0070," which two different ADRs shared — the number was disambiguated, and the Todo-navigation ADR is now 0076 |
| NAV-03 | Below 900px there is no sidebar and the bottom bar remains | ADR 0076 | `chat-shell-layout.test.tsx` | `built` | Held by a test, not an assertion — this is what keeps Android intact. **It was not enough.** This row holds that the bottom bar *remains*; nothing held that every destination is reachable *from* it, and Upcoming (#223) was added to `todo-sidebar.tsx` alone — leaving it unreachable on any touch device for the life of this branch, since the only other path was a keyboard shortcut. Todo has two navigations, not one, and a new destination has to enter both. **Citation corrected (issue #248):** see NAV-02's own note on the ADR renumbering |
| NAV-04 | Scoped `<nav aria-label="Todo">` with real hrefs and `aria-current` | `chat-list.tsx` precedent | `todo-sidebar.tsx` | `built` | |
| NAV-05 | Upcoming day headings read `10 Sep ‧ Today ‧ Thursday`; only today and tomorrow get a relative word | scheduler-and-priority.md §9 | `task-views.ts` `upcomingDayHeading()` | `built` | |
| NAV-06 | "Filters & Labels" is one destination covering both, with a combined count | live sidebar | `todo-sidebar.tsx` links to `/todo/filters`, which now renders both a Filters list and a Labels section (`filters-view.tsx`); the sidebar's own count is `filters.length + labels.length` | `built` | #229's second half. Todoist's own screen puts full inline editing for both nouns on the one page; here, Labels' own full create/rename/recolour/delete lives one hop away at `/todo/labels` (`labels-view.tsx`), reached via this page's "Manage Labels" link — the destination and the count now genuinely cover both, but the *editing* surface is one link deeper for Labels than for Filters. Not driven side by side against the real app |
| NAV-07 | "Add task" in the sidebar opens Quick Add from anywhere | live sidebar | links to `/todo/inbox` | `divergent` | No global Quick Add exists yet; #226 owns it |
| NAV-11 | Todo's Activity view has no path to it from the sidebar at ≥900px | live app | `todo-sidebar.tsx` now links `/todo/activity` (a `CountRow`, no count badge — Activity is a log, not a pending count) | `built` | **Fixed by issue #248.** Previously `/todo/activity` was linked only from `todo-nav.tsx:78` (which hides itself at ≥900px, per NAV-03) and from inside a Project (`project-view.tsx:217-218`); `todo-sidebar.tsx` — the nav that replaces `todo-nav` at that width — had no Activity entry anywhere. The identical defect class NAV-03's own note already names for Upcoming: a destination reachable from one navigation but not the other is unreachable on desktop, since the only other path is a keyboard shortcut Activity has none of. Not `matched` — nobody has driven the two sidebars side by side for this row |
| NAV-12 | Todoist's real add-task affordance is a **static label** that expands into Quick Add on click, with its own date/priority/project pickers, focus handling and escape-to-collapse | quick-add.md § Quick Add chrome | — | `divergent` | **Named, deliberately deferred — issue #252's own scope note.** meologue's field is always-mounted (never unmounts to a bare label) and its Add button is rendered-but-disabled rather than absent, so `add-task-form.test.tsx` and `apps/e2e/tests/todo.spec.ts` keep working against a field that exists before any click. Folding the click-to-reveal composer in here would break both for no parity gain that NAV-10's position/weight fix doesn't already deliver on its own. Owned by a future ticket, not by #252 |
| THEME-06 | The token scope must reach **portaled** overlays — Radix mounts every dialog, menu, sheet and popover into `document.body` | n/a (our own architecture) | `data-surface` written to `documentElement` by `chat-shell-layout.tsx` | `matched` | Found by measuring: the detail dialog rendered `insideScope: false`, painted `oklch(0.205 0 0)` and set in Geist, unthemed since #223. A subtree scope cannot cover a portal; `documentElement` is above every portal by construction |
| THEME-07 | The dark override must use the compound selector `.dark[data-surface="todo"]`, not only the descendant form | n/a (our own architecture) | `index.css` carries both | `matched` | Moving the attribute to `documentElement` put `.dark` and `data-surface` on the **same** element, so the descendant selector stopped matching and Todo silently fell back to the light palette. Caught by reading white where the page was `rgb(31,31,31)` |
| DET-14 | The detail modal is background `rgb(31,31,31)` — the same ground as the content pane, not a raised surface — radius **10px**, no border, shadow `rgba(0,0,0,.16) 0 2px 8px`, 864x772 | measured live in Todoist | `--card`/`--popover` now follow `--background`; `task-detail-view.tsx`'s wide dialog now carries `rounded-[10px]` | `built` | Background and font already matched. Radius fixed in #229: `rounded-xl` (14px) → `rounded-[10px]`, measured in-app via `getComputedStyle` after the change |
| STR-01 | Delete a Project: dialog reads "Delete project? The [name] project and all its tasks will be permanently deleted. This action cannot be undone.", buttons Cancel/Delete | quick-add.md § "Destructive confirmation wording" | `project-view.tsx`'s new `ConfirmDialog`, verbatim; `use-projects.ts`'s new `removeProject` reaches `ProjectStore.removeProject` (existed since #171, wired to no UI until now) | `built` | Driven in a real browser: created a Project, opened it, deleted it, read the dialog text back |
| STR-02 | A Project's colour is changeable after creation, not fixed at creation time | 171-brief.md's own acceptance criteria (create/rename/recolour/favourite/archive/delete) | `project-view.tsx`'s header colour `<select>`, reaching `ProjectStore.setProjectColour` (existed since #171, wired to no UI until now) via `use-projects.ts`'s `setProjectColour` | `built` | Driven in a real browser: opened a Project, changed its colour, saw the dot update |
| STR-03 | Delete a Label: dialog reads "Delete label? The [name] label will be permanently deleted.", buttons Cancel/Delete | quick-add.md § "Destructive confirmation wording" | `labels-view.tsx`'s new `ConfirmDialog`, verbatim | `built` | Driven in a real browser: created a Label, deleted it, read the dialog text back |
| STR-04 | A Label can be created, renamed and recoloured from a management screen, not only as a side effect of `@name` in quick-add | CONTEXT.md's Label entry; `use-labels.ts`'s own pre-#229 header comment naming this exact gap | New `/todo/labels` route, `labels-view.tsx` (create form, inline rename-on-blur, per-row colour `<select>`); `use-labels.ts`'s new `addLabel`/`renameLabel`/`setLabelColour`, reaching `LabelStore.rename`/`setColour` (existed since #170, wired to no UI until now) | `built` | Driven in a real browser: created "Test", renamed it, recoloured it |
| STR-05 | Todoist's Label options menu (Edit · Add to favorites · Move to shared labels · Copy link to label · Delete), reproduced minus what doesn't apply | quick-add.md § "Menus seen in passing" | `labels-view.tsx`'s own header comment: only Edit (inline rename field) and Delete survive — no Label favourite or shared Labels exist in this app (`label-types.ts` carries neither field), and no per-Label route exists for a "copy link" to point at | `built` | Not a literal menu widget — inline controls doing the same two jobs. Not driven against the real app (this app has no Label favourite/sharing to compare) |
| STR-06 | Create/rename/recolour/delete for Filters | 185-brief.md's own acceptance criteria | `filter-view.tsx`/`filters-view.tsx` already carried all four before this ticket (`addFilter`/`renameFilter`/`setFilterColour`/`removeFilter`, `use-filters.ts`) | `built` | Verified reachable, not newly built — this ticket's own gap analysis found Filters already complete; Filter carries no `favourite`/`archived` field (`filter-types.ts`), so those two are not applicable, matching Project's own type being the one of the three with both flags |
| STR-07 | Sections' existing create/rename/reorder/archive/delete (built in #171) is reachable and consistent with this ticket's own new Project/Label controls | 171-brief.md's own acceptance criteria | `project-view.tsx` — unchanged by this ticket, confirmed reachable from `/todo/projects/:id` and left as-is (the destructive count-then-confirm shape it already had is what this ticket's own new Project-delete confirmation was modelled on) | `built` | Read, not rebuilt. Section's own delete dialog wording ("This destroys N Tasks…") predates the verbatim Todoist wording table this ticket works from and was left untouched — not a row that table names |
