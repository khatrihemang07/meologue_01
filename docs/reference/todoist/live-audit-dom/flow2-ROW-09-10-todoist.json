{
  "app": "todoist",
  "rows": ["ROW-09", "ROW-10"],
  "identityCheck": "read each row's own #task-<id>-info-tags div outerHTML, and separately the priority-only row's own task-info-tags (empty) to check whether priority renders as a text badge at all",
  "priorityBadge": {
    "task": "ZZ probe P1",
    "tagsHTML": "<div id=\"task-...-info-tags\" data-testid=\"task-info-tags\" class=\"pktBVPO d2b5021f\"></div>",
    "verdict": "EMPTY -- Todoist shows priority ONLY via the checkbox ring colour, never as a separate text/pill badge in the row's metadata line. meologue renders a literal 'P1' text badge in its metadata line for every non-default priority (see flow2-ROW-08-09-10-meologue.json). This is a genuine, newly-discovered ROW-09/10 divergence."
  },
  "labelBadge": {
    "task": "ZZ probe labels",
    "tagsHTML": "<div ...info-tags...><span class=\"bNObnB3\"><a href=\"/app/label/zz-probe-2185027178\" style=\"color: var(--product-library-meta-charcoal-tint);\"><svg/><span class=\"simple_content\">zz-probe</span></a></span></div>",
    "verdict": "the Label badge IS a real <a href='/app/label/...'> link on Todoist, not a plain span -- meologue's own Label badge is a plain non-link <span> with a colour dot (see flow2-ROW-08-09-10-meologue.json). Another newly-discovered divergence, same class as ROW-08's comment-badge-is-a-link finding."
  },
  "projectBadge": {
    "task": "ZZ probe other project (moved into Getting Started project)",
    "note": "measured from WITHIN that project's own view -- task-info-tags was empty there. Todoist likely only shows a Project badge in a CROSS-project view (search results, a Filter, 'Today'/'Upcoming'), not when already looking at that Project's own task list. Not measurable this session without a cross-project view fixture; recorded as a gap, same class as row-and-detail.md's own original ROW-10 gap."
  },
  "subtaskCountBadge": {
    "task": "ZZ probe parent (1 sub-task: ZZ probe child)",
    "note": "the parent row's own task-info-tags was not captured before virtualization scrolled it out of the live DOM this session; row-and-detail.md never established whether Todoist shows a sub-task COUNT badge on the row at all (its own known behaviour is nesting the child inline underneath the parent, indented, rather than a count summary) -- this remains an open gap, not contradicted or confirmed this session."
  }
}
