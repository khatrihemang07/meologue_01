{
  "row": "QA-16 - Todoist-only chrome, Quick Add geometry",
  "ledger_reference_value_from_quick-add.md": "580x97px at rest, radius 12px, padding 16px (dark theme)",
  "todoist_measured_live_2026-09-11": {
    "entry_point": ".plus_add_button in the Inbox list (the disambiguated global affordance per task instructions)",
    "resulting_composer": "an INLINE row rendered within the task list itself (an <li class=\"manager indent_1 manager--add\"> box), NOT a floating/fixed modal - getComputedStyle(position) is 'relative' and no [role=dialog] element exists anywhere in the document while it is open",
    "measured_geometry": {
      "widthPx": 800,
      "heightPx": 89,
      "borderRadius": "12px",
      "background": "rgb(40, 40, 40)",
      "border": "1px solid rgb(112, 112, 112)",
      "padding": "12px"
    }
  },
  "note": "The measured geometry (800x89, 12px radius, 12px padding) does not match the 580x97/16px-padding figure captured in quick-add.md. Radius (12px) matches; width, height and padding do not. This is consistent with the ledger's own acknowledged live-product drift (see SCHED-02's note on Todoist's UI changing between captures) OR with quick-add.md having captured a different Quick Add entry point (a true centered overlay dialog, e.g. via a global keyboard shortcut) than the in-list '+Add task' row reached via .plus_add_button. Both explanations are plausible; not disambiguated here. Reporting the number actually read rather than the older captured one.",
  "meologue": "Not measured for direct comparison per task scope (QA-16 is Todoist-only chrome) - meologue's composer is a top-of-list bordered field, structurally different, see ledger's own NAV-10 for that comparison.",
  "verdict": "blocked/inconclusive - live measurement diverges from the cited capture; cause not determined without checking whether a distinct global-shortcut Quick Add dialog still exists"
}
