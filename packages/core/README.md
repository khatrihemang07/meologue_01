# @meologue/core

Platform-agnostic core package, shared between the web app and any future clients.

`src/generated/` holds the wire contract types generated from the Rust server's OpenAPI spec
(see `server/README.md`) — auto-generated and committed, not hand-edited.
